diff --git a/css/style.css b/css/style.css
index 4c85530..bd37bd5 100755
--- a/css/style.css
+++ b/css/style.css
@@ -6,8 +6,8 @@
 
    ============================================ */
 
-@import url('https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400;700;900&family=Montserrat:wght@100..900&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap');
-@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@100..900&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap');
+@import url('https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400;700;900&family=Cormorant+Garamond:ital,wght@0,300..700;1,300..700&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap');
+@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300..700;1,300..700&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap');
 
 /* ---------- Custom Fonts ---------- */
 
@@ -34,23 +34,58 @@
   /* Placeholder path, update with your font file */
 }
 
+@font-face {
+  font-family: 'Drose';
+  font-weight: normal;
+  font-style: normal;
+  font-display: swap;
+  src: url('../assets/fonts/Drose.otf') format('opentype');
+}
+
+/* New Cormorant Garamond font */
+@font-face {
+  font-family: 'Cormorant Garamond';
+  font-weight: normal;
+  font-style: normal;
+  font-display: swap;
+  src: url('../assets/fonts/Cormorant GaramondFancyAestheticSerif-lx0O0.otf') format('opentype');
+}
+
 /* ---------- CSS Custom Properties ---------- */
 
 :root {
-  --clr-bg: #FAF6F0;
-  --clr-bg-deep: #F3ECE2;
-  --clr-bg-cream: #EDE7DC;
-  --clr-bg-warm: #FCFAF7;
-  --clr-bg-footer: #7D2826;
-  --clr-text: #222222;
-  --clr-text-muted: #666666;
-  --clr-text-subtle: #999999;
-  --clr-text-light: #ffffff;
-  --clr-border: #E6E0D5;
-  --clr-border-light: #D9D2C6;
-  --clr-overlay: rgba(0, 0, 0, 0.4);
-  --ff-display: 'Foreshadow Regular', 'Georgia', serif;
-  --ff-body: 'Foreshadow Text', 'Georgia', serif;
+  /* Brand Color Palette */
+  --clr-brand-red: #7D2826;
+  /* Elegant Crimson Red */
+  --clr-brand-cream: #EFE9D7;
+  /* Premium Linen Cream Background */
+  --clr-brand-sage: #898861;
+  /* Organic Accent Sage Green */
+  --clr-brand-dark: #343723;
+  /* Deep Olive Charcoal Text */
+  --clr-brand-navy: #343772;
+  /* Brand Alternate Navy */
+
+  /* Semantic Color Mapping */
+  --clr-bg: var(--clr-brand-cream);
+  --clr-bg-deep: #E2DCC9;
+  /* Warm slightly deeper beige */
+  --clr-bg-cream: var(--clr-brand-cream);
+  --clr-bg-warm: #F8F5EB;
+  /* Soft warm off-white */
+  --clr-bg-footer: var(--clr-brand-red);
+  --clr-text: var(--clr-brand-dark);
+  --clr-text-muted: #5E6347;
+  /* Soft olive-tinted gray */
+  --clr-text-subtle: var(--clr-brand-sage);
+  --clr-text-light: #F8F5EB;
+  --clr-border: #D1CCA9;
+  /* Sophisticated cream-toned border */
+  --clr-border-light: var(--clr-brand-sage);
+  --clr-overlay: rgba(52, 55, 35, 0.45);
+  /* Forest dark tinted overlay */
+  --ff-display: 'Cormorant Garamond', 'Georgia', serif;
+  --ff-body: 'Cormorant Garamond', 'Georgia', serif;
   --fs-hero: clamp(2.8rem, 7vw, 6rem);
   --fs-h1: clamp(2rem, 4.5vw, 3.5rem);
   --fs-h2: clamp(1.5rem, 3vw, 2.5rem);
@@ -65,8 +100,8 @@
   --space-lg: 4rem;
   --space-xl: 6rem;
   --space-2xl: 10rem;
-  --max-width: 1200px;
-  --nav-height: 90px;
+  --max-width: 75rem;
+  --nav-height: 5.625rem;
   --ease-smooth: cubic-bezier(0.25, 0.46, 0.45, 0.94);
   --ease-out-expo: cubic-bezier(0.19, 1, 0.22, 1);
   --dur-fast: 0.3s;
@@ -74,6 +109,18 @@
   --dur-slow: 1s;
 }
 
+/* ---------- Large Monitor Proportional Scaling ---------- */
+@media (min-width: 1440px) {
+  html {
+    font-size: 1.111vw !important;
+  }
+}
+@media (min-width: 2560px) {
+  html {
+    font-size: 28.4px !important;
+  }
+}
+
 /* ---------- Reset & Base ---------- */
 
 *,
@@ -109,7 +156,15 @@ body {
 
 h1,
 h2,
-h3,
+h3 {
+  font-family: var(--ff-display);
+  font-weight: 100;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  line-height: 1.3;
+  color: var(--clr-brand-red);
+}
+
 h4,
 h5,
 h6 {
@@ -136,7 +191,7 @@ h3 {
 
 p {
   font-family: var(--ff-body);
-  font-weight: 100;
+  font-weight: 400;
   line-height: 1.9;
   color: var(--clr-text);
 }
@@ -246,7 +301,7 @@ ol {
 .preloader {
   position: fixed;
   inset: 0;
-  background: #000000;
+  background: #EFE9D7;
   z-index: 100000;
   display: flex;
   align-items: center;
@@ -274,7 +329,7 @@ ol {
 
 .preloader-overlay {
   position: relative;
-  z-index: 2;
+  z-index: 10;
   display: flex;
   flex-direction: column;
   align-items: center;
@@ -286,59 +341,141 @@ ol {
   transform: translateY(20px) scale(0.95);
   transition: opacity 1.2s cubic-bezier(0.19, 1, 0.22, 1), transform 1.2s cubic-bezier(0.19, 1, 0.22, 1);
   pointer-events: none;
+  /* CSS-only fallback: auto-reveal after 2.5s even if JS fails */
+  animation: preloaderOverlayFallback 0s 2.5s forwards;
+}
+
+@keyframes preloaderOverlayFallback {
+  to {
+    opacity: 1;
+    transform: translateY(0) scale(1);
+    pointer-events: auto;
+  }
 }
 
 .preloader-overlay.visible {
   opacity: 1;
   transform: translateY(0) scale(1);
   pointer-events: auto;
+  /* Cancel the fallback animation once JS has applied the class */
+  animation: none;
 }
 
+/* --- Preloader Logo: elegant entrance animation --- */
 .preloader-logo {
-  height: 64px;
+  height: 120px;
   width: auto;
-  filter: invert(1);
-  margin-bottom: 0.5rem;
+  filter: none;
+  margin-bottom: 0.8rem;
+  opacity: 0;
+  transform: scale(0.6) translateY(30px);
+  animation: preloaderLogoEntrance 1.4s cubic-bezier(0.16, 1, 0.3, 1) 0.3s forwards;
   transition: transform 1s cubic-bezier(0.19, 1, 0.22, 1), opacity 0.5s ease;
 }
 
+@keyframes preloaderLogoEntrance {
+  0% {
+    opacity: 0;
+    transform: scale(0.6) translateY(30px);
+    filter: blur(8px);
+  }
+
+  50% {
+    opacity: 1;
+    filter: blur(0px);
+  }
+
+  80% {
+    transform: scale(1.04) translateY(-4px);
+  }
+
+  100% {
+    opacity: 1;
+    transform: scale(1) translateY(0);
+    filter: blur(0px);
+  }
+}
+
+/* --- Preloader Brand Text --- */
 .preloader-brand {
   font-family: var(--ff-display);
   font-size: 14px;
-  color: #ffffff;
+  color: var(--clr-brand-dark, #343723);
   letter-spacing: 0.4em;
   text-transform: uppercase;
   font-weight: normal;
   line-height: 1;
+  opacity: 0;
+  transform: translateY(12px);
+  animation: preloaderTextFadeIn 0.9s cubic-bezier(0.16, 1, 0.3, 1) 1.0s forwards;
+}
+
+@keyframes preloaderTextFadeIn {
+  to {
+    opacity: 1;
+    transform: translateY(0);
+  }
 }
 
+/* --- Preloader Tagline --- */
 .preloader-tagline {
   font-family: var(--ff-body);
   font-size: 9px;
-  color: var(--clr-text-muted, #888888);
+  color: var(--clr-text-muted, #5E6347);
   letter-spacing: 0.25em;
   text-transform: uppercase;
   margin-bottom: 1.5rem;
+  opacity: 0;
+  transform: translateY(12px);
+  animation: preloaderTextFadeIn 0.9s cubic-bezier(0.16, 1, 0.3, 1) 1.3s forwards;
 }
 
+/* --- Enter Button: dark-on-cream elegant style --- */
 .preloader-enter-btn {
   font-family: var(--ff-display);
   font-size: 10px;
-  color: #ffffff;
+  color: var(--clr-brand-dark, #343723);
   background: transparent;
-  border: 1.5px solid #ffffff;
+  border: 1.5px solid var(--clr-brand-dark, #343723);
   padding: 0.9rem 3.2rem;
   letter-spacing: 0.3em;
   text-transform: uppercase;
   cursor: pointer;
-  transition: all 0.3s ease;
+  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
   outline: none;
+  opacity: 0;
+  transform: translateY(16px);
+  animation: preloaderBtnEntrance 0.9s cubic-bezier(0.16, 1, 0.3, 1) 1.6s forwards;
+  position: relative;
+  overflow: hidden;
+}
+
+@keyframes preloaderBtnEntrance {
+  to {
+    opacity: 1;
+    transform: translateY(0);
+  }
+}
+
+.preloader-enter-btn::before {
+  content: '';
+  position: absolute;
+  inset: 0;
+  background: var(--clr-brand-dark, #343723);
+  transform: scaleX(0);
+  transform-origin: left;
+  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
+  z-index: -1;
 }
 
 .preloader-enter-btn:hover {
-  background: #ffffff;
-  color: #000000;
-  box-shadow: 0 0 20px rgba(255, 255, 255, 0.4);
+  color: #EFE9D7;
+  border-color: var(--clr-brand-dark, #343723);
+  box-shadow: 0 4px 24px rgba(52, 55, 35, 0.15);
+}
+
+.preloader-enter-btn:hover::before {
+  transform: scaleX(1);
 }
 
 .preloader-hint {
@@ -346,12 +483,101 @@ ol {
   bottom: 6%;
   font-family: var(--ff-body);
   font-size: 9px;
-  color: rgba(255, 255, 255, 0.4);
+  color: rgba(52, 55, 35, 0.35);
   letter-spacing: 0.2em;
   text-transform: uppercase;
   pointer-events: none;
   z-index: 2;
   transition: opacity 0.6s ease;
+  opacity: 0;
+  animation: preloaderTextFadeIn 0.8s ease 2.0s forwards;
+}
+
+/* --- Preloader Task Instructions Box --- */
+.preloader-instructions {
+  margin-top: 1.5rem;
+  max-width: 620px;
+  width: 100%;
+  max-height: 200px;
+  overflow-y: auto;
+  background: rgba(255, 255, 255, 0.45);
+  border: 1px solid rgba(52, 55, 35, 0.12);
+  border-radius: 8px;
+  padding: 1.25rem;
+  box-shadow: 0 8px 32px rgba(52, 55, 35, 0.04);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
+  text-align: left;
+  font-family: var(--ff-body);
+  font-size: 12px;
+  color: var(--clr-brand-dark, #343723);
+  line-height: 1.6;
+  opacity: 0;
+  transform: translateY(15px);
+  animation: preloaderTextFadeIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) 1.9s forwards;
+  box-sizing: border-box;
+}
+
+.preloader-instructions h3 {
+  font-family: var(--ff-display);
+  font-size: 13px;
+  margin-top: 0;
+  margin-bottom: 0.8rem;
+  text-transform: uppercase;
+  letter-spacing: 0.15em;
+  border-bottom: 1px solid rgba(52, 55, 35, 0.1);
+  padding-bottom: 0.4rem;
+  color: var(--clr-brand-dark, #343723);
+}
+
+.preloader-instructions p {
+  margin: 0.4rem 0;
+}
+
+.preloader-instructions ul {
+  margin: 0.4rem 0;
+  padding-left: 1.2rem;
+}
+
+.preloader-instructions li {
+  margin: 0.2rem 0;
+}
+
+.preloader-instructions code {
+  background: rgba(52, 55, 35, 0.08);
+  padding: 0.15rem 0.35rem;
+  border-radius: 4px;
+  font-family: monospace;
+  font-size: 11px;
+}
+
+.preloader-instructions a {
+  color: var(--clr-brand-dark, #343723);
+  text-decoration: underline;
+  font-weight: 500;
+  transition: opacity 0.2s ease;
+}
+
+.preloader-instructions a:hover {
+  opacity: 0.7;
+}
+
+/* Custom Scrollbar for instructions card */
+.preloader-instructions::-webkit-scrollbar {
+  width: 5px;
+}
+
+.preloader-instructions::-webkit-scrollbar-track {
+  background: transparent;
+}
+
+.preloader-instructions::-webkit-scrollbar-thumb {
+  background: rgba(52, 55, 35, 0.15);
+  border-radius: 10px;
+}
+
+.preloader-instructions::-webkit-scrollbar-thumb:hover {
+  background: rgba(52, 55, 35, 0.3);
 }
 
 /* Preloader Exit Animation States */
@@ -361,6 +587,7 @@ ol {
   transform: translateY(-20px) scale(1.05);
   transition: opacity 0.8s cubic-bezier(0.19, 1, 0.22, 1), transform 0.8s cubic-bezier(0.19, 1, 0.22, 1);
   pointer-events: none;
+  animation: none;
 }
 
 .preloader.exiting .preloader-canvas-container {
@@ -376,7 +603,7 @@ ol {
 .preloader-flash {
   position: absolute;
   inset: 0;
-  background: radial-gradient(circle, rgba(255, 255, 255, 1) 0%, rgba(255, 255, 255, 0.8) 40%, rgba(0, 0, 0, 0) 80%);
+  background: radial-gradient(circle, rgba(239, 233, 215, 1) 0%, rgba(239, 233, 215, 0.9) 40%, rgba(239, 233, 215, 0) 80%);
   opacity: 0;
   z-index: 100001;
   pointer-events: none;
@@ -402,7 +629,7 @@ ol {
 /* Responsiveness for Preloader */
 @media (max-width: 768px) {
   .preloader-logo {
-    height: 48px;
+    height: 80px;
   }
 
   .preloader-brand {
@@ -695,6 +922,18 @@ ol {
   transition-delay: 0.6s;
 }
 
+.stagger-7 {
+  transition-delay: 0.7s;
+}
+
+.stagger-8 {
+  transition-delay: 0.8s;
+}
+
+.stagger-9 {
+  transition-delay: 0.9s;
+}
+
 /* ---------- Page Transition ---------- */
 
 .page-transition {
@@ -744,10 +983,10 @@ ol {
 .section-label {
   font-family: var(--ff-body);
   font-size: var(--fs-label);
-  font-weight: 100;
-  letter-spacing: 0.3em;
+  font-weight: 500;
+  letter-spacing: 0.35em;
   text-transform: uppercase;
-  color: var(--clr-text-muted);
+  color: var(--clr-brand-sage);
   margin-bottom: var(--space-sm);
   display: block;
 }
@@ -784,8 +1023,8 @@ ol {
   left: 0;
   right: 0;
   width: 100%;
-  height: 180px;
-  /* Restored to hold the 160px logo nicely */
+  height: 11.25rem;
+  /* Restored to hold the 10rem logo nicely */
   z-index: 1000;
   display: grid;
   grid-template-columns: 1fr auto 1fr;
@@ -797,7 +1036,7 @@ ol {
 
 .nav.scrolled {
   position: fixed;
-  height: 100px;
+  height: 6.25rem;
   background: rgba(125, 40, 38, 0.95);
   backdrop-filter: blur(12px);
   -webkit-backdrop-filter: blur(12px);
@@ -805,6 +1044,13 @@ ol {
   box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
 }
 
+/* Hide nav on Gallery page during scroll */
+.gallery-body .nav.scrolled {
+  opacity: 0;
+  transform: translateY(-100%);
+  pointer-events: none;
+}
+
 .nav.nav--dark .nav-link,
 .nav.nav--dark .nav-cta {
   color: var(--clr-text-light);
@@ -834,7 +1080,7 @@ ol {
 }
 
 .nav-logo-img {
-  height: 160px;
+  height: 10rem;
   /* Restored to 5x size */
   width: auto;
   display: block;
@@ -842,7 +1088,7 @@ ol {
 }
 
 .nav.scrolled .nav-logo-img {
-  height: 80px;
+  height: 5rem;
   /* Restored to medium size when scrolled */
 }
 
@@ -899,8 +1145,8 @@ ol {
 
 .nav-link {
   font-family: var(--ff-body);
-  font-size: 11px;
-  font-weight: 100;
+  font-size: 0.6875rem;
+  font-weight: 500;
   letter-spacing: 0.2em;
   text-transform: uppercase;
   color: var(--clr-text);
@@ -926,6 +1172,25 @@ ol {
   width: 100%;
 }
 
+.nav-link.active {
+  background: transparent !important;
+  background-color: transparent !important;
+  border-radius: 0 !important;
+  width: auto !important;
+  height: auto !important;
+  display: inline-block !important;
+  padding: 0.25rem 0 !important;
+  color: var(--clr-text) !important;
+}
+
+.nav.nav--dark .nav-link.active {
+  color: var(--clr-text-light) !important;
+}
+
+.nav.scrolled .nav-link.active {
+  color: var(--clr-text-light) !important;
+}
+
 .nav-link:hover {
   opacity: 0.7;
 }
@@ -933,7 +1198,7 @@ ol {
 .nav-cta {
   font-family: var(--ff-body);
   font-size: 11px;
-  font-weight: 100;
+  font-weight: 500;
   letter-spacing: 0.2em;
   text-transform: uppercase;
   color: var(--clr-text);
@@ -962,85 +1227,257 @@ ol {
   opacity: 0.7;
 }
 
-/* Hamburger */
+/* ---------- Navigation Dropdown (More Section) ---------- */
 
-.nav-hamburger {
-  display: none;
-  flex-direction: column;
-  gap: 6px;
-  cursor: pointer;
+.nav-dropdown {
   position: relative;
-  z-index: 1001;
-  padding: 8px;
-  grid-column: 3;
-  justify-self: flex-end;
 }
 
-.nav-hamburger span {
-  width: 28px;
-  height: 1.5px;
-  background: var(--clr-text);
-  transition: all var(--dur-normal) var(--ease-out-expo);
-  transform-origin: center;
+.nav-dropdown-menu {
+  position: absolute;
+  top: calc(100% + 0.9375rem);
+  left: 50%;
+  transform: translateX(-50%) translateY(0.625rem);
+  list-style: none;
+  padding: 0.8rem 0;
+  min-width: 9.375rem;
+  background: rgba(18, 18, 18, 0.96);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
+  border: 1px solid rgba(255, 255, 255, 0.08);
+  box-shadow: 0 12px 35px rgba(0, 0, 0, 0.3);
+  opacity: 0;
+  visibility: hidden;
+  pointer-events: none;
+  z-index: 1010;
+  text-align: center;
+  transition: opacity var(--dur-fast) var(--ease-smooth), transform var(--dur-fast) var(--ease-smooth), visibility var(--dur-fast);
 }
 
-.nav.nav--dark .nav-hamburger span {
-  background: var(--clr-text-light);
+.nav-dropdown-menu::before {
+  content: '';
+  position: absolute;
+  top: -0.9375rem;
+  left: 0;
+  width: 100%;
+  height: 0.9375rem;
+  background: transparent;
 }
 
-.nav.scrolled .nav-hamburger span {
-  background: var(--clr-text-light);
-  /* White spans over deep red background */
+.nav-dropdown:hover .nav-dropdown-menu,
+.nav-dropdown-menu:hover {
+  opacity: 1;
+  visibility: visible;
+  pointer-events: auto;
+  transform: translateX(-50%) translateY(0);
 }
 
-.nav-hamburger.open span {
-  background: var(--clr-text) !important;
+.nav-dropdown-item {
+  display: block;
+  padding: 0.6rem 1.5rem;
+  font-family: var(--ff-body);
+  font-size: 0.625rem;
+  font-weight: 500;
+  letter-spacing: 0.25em;
+  text-transform: uppercase;
+  color: rgba(255, 255, 255, 0.85);
+  transition: color 0.3s ease, background-color 0.3s ease;
+  position: relative;
 }
 
-.nav-hamburger.open span:nth-child(1) {
-  transform: rotate(45deg) translate(5px, 5px);
+.nav-dropdown-item:hover {
+  color: var(--clr-text-light);
+  background-color: rgba(255, 255, 255, 0.06);
+  opacity: 1;
 }
 
-.nav-hamburger.open span:nth-child(2) {
-  opacity: 0;
-  transform: scaleX(0);
+.nav-dropdown-item.active {
+  color: var(--clr-text-light);
+  font-weight: bold;
 }
 
-.nav-hamburger.open span:nth-child(3) {
-  transform: rotate(-45deg) translate(5px, -5px);
+/* Scrolled Nav Dropdown (Deep Red Theme) */
+.nav.scrolled .nav-dropdown-menu {
+  background: rgba(95, 28, 26, 0.98);
+  border: 1px solid rgba(255, 255, 255, 0.12);
+  box-shadow: 0 12px 35px rgba(0, 0, 0, 0.4);
 }
 
-/* Responsive adjustments */
-@media (max-width: 1024px) {
-  .nav {
-    display: flex;
-    justify-content: space-between;
-    align-items: center;
-    height: 90px;
-    padding: 0 1.5rem;
-  }
+.nav.scrolled .nav-dropdown-item {
+  color: rgba(255, 255, 255, 0.9);
+}
 
-  .nav.scrolled {
-    height: 70px;
-    background: rgba(125, 40, 38, 0.9) !important;
-    backdrop-filter: blur(12px) !important;
-    -webkit-backdrop-filter: blur(12px) !important;
-    border-bottom: 1px solid rgba(255, 255, 255, 0.15) !important;
-    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2) !important;
-  }
+.nav.scrolled .nav-dropdown-item:hover {
+  background-color: rgba(255, 255, 255, 0.08);
+}
 
-  .nav-links--left,
-  .nav-links--right {
-    display: none !important;
-  }
+/* Non-Dark Unscrolled Nav Dropdown (Light Theme for pages like portfolio/gallery) */
+.nav:not(.nav--dark):not(.scrolled) .nav-dropdown-menu {
+  background: rgba(250, 246, 240, 0.98);
+  border: 1px solid rgba(0, 0, 0, 0.08);
+  box-shadow: 0 12px 35px rgba(0, 0, 0, 0.08);
+}
 
-  .nav-hamburger {
-    display: flex !important;
-  }
+.nav:not(.nav--dark):not(.scrolled) .nav-dropdown-item {
+  color: var(--clr-text);
+}
 
-  .nav-brand-container {
-    justify-content: flex-start;
-    align-items: center;
+.nav:not(.nav--dark):not(.scrolled) .nav-dropdown-item:hover {
+  background-color: rgba(0, 0, 0, 0.04);
+}
+
+/* ---------- Mobile Overlay Submenu (More Section) ---------- */
+
+.nav-overlay-dropdown {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  width: 100%;
+}
+
+.nav-overlay-dropdown-menu {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 1.2rem;
+  max-height: 0;
+  overflow: hidden;
+  opacity: 0;
+  width: 100%;
+  transition: max-height 0.4s var(--ease-smooth), opacity 0.4s ease, margin-top 0.4s ease;
+}
+
+.nav-overlay-dropdown-menu.open {
+  max-height: 200px;
+  opacity: 1;
+  margin-top: 1.2rem;
+  margin-bottom: 0.6rem;
+}
+
+.nav-overlay-dropdown-link {
+  font-family: var(--ff-display);
+  font-size: clamp(1.8rem, 4.5vw, 3rem);
+  font-weight: 100;
+  letter-spacing: 0.15em;
+  color: var(--clr-text);
+  text-transform: uppercase;
+  transition: opacity var(--dur-fast), color var(--dur-fast);
+  opacity: 0.7;
+}
+
+@media screen and (max-width: 991px) {
+  .nav-overlay-dropdown-link {
+    font-size: 7rem !important; /* Larger size for mobile viewport */
+  }
+  .nav-overlay-dropdown-menu.open {
+    max-height: 350px !important;
+  }
+}
+
+.nav-overlay-dropdown-link:hover {
+  opacity: 1;
+}
+
+.nav-overlay-dropdown-link.active {
+  opacity: 1;
+  font-weight: 600;
+}
+
+#mobile-more-toggle {
+  cursor: pointer;
+  display: inline-block;
+}
+
+#mobile-more-toggle::after {
+  content: ' +';
+  font-family: inherit;
+  transition: transform 0.3s ease;
+  display: inline-block;
+}
+
+#mobile-more-toggle.active::after {
+  content: ' −';
+}
+
+/* Hamburger */
+
+.nav-hamburger {
+  display: none;
+  flex-direction: column;
+  gap: 6px;
+  cursor: pointer;
+  position: relative;
+  z-index: 1001;
+  padding: 8px;
+  grid-column: 3;
+  justify-self: flex-end;
+}
+
+.nav-hamburger span {
+  width: 28px;
+  height: 1.5px;
+  background: var(--clr-text);
+  transition: all var(--dur-normal) var(--ease-out-expo);
+  transform-origin: center;
+}
+
+.nav.nav--dark .nav-hamburger span {
+  background: var(--clr-text-light);
+}
+
+.nav.scrolled .nav-hamburger span {
+  background: var(--clr-text-light);
+  /* White spans over deep red background */
+}
+
+.nav-hamburger.open span {
+  background: var(--clr-text) !important;
+}
+
+.nav-hamburger.open span:nth-child(1) {
+  transform: rotate(45deg) translate(5px, 5px);
+}
+
+.nav-hamburger.open span:nth-child(2) {
+  opacity: 0;
+  transform: scaleX(0);
+}
+
+.nav-hamburger.open span:nth-child(3) {
+  transform: rotate(-45deg) translate(5px, -5px);
+}
+
+/* Responsive adjustments */
+@media (max-width: 1024px) {
+  .nav {
+    display: flex;
+    justify-content: space-between;
+    align-items: center;
+    height: 90px;
+    padding: 0 1.5rem;
+  }
+
+  .nav.scrolled {
+    height: 70px;
+    background: rgba(125, 40, 38, 0.9) !important;
+    backdrop-filter: blur(12px) !important;
+    -webkit-backdrop-filter: blur(12px) !important;
+    border-bottom: 1px solid rgba(255, 255, 255, 0.15) !important;
+    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2) !important;
+  }
+
+  .nav-links--left,
+  .nav-links--right {
+    display: none !important;
+  }
+
+  .nav-hamburger {
+    display: flex !important;
+  }
+
+  .nav-brand-container {
+    justify-content: flex-start;
+    align-items: center;
     height: 100%;
   }
 
@@ -1051,11 +1488,11 @@ ol {
   }
 
   .nav-logo-img {
-    height: 60px;
+    height: 3.75rem;
   }
 
   .nav.scrolled .nav-logo-img {
-    height: 45px;
+    height: 2.8125rem;
   }
 
   .nav-brand-text {
@@ -1115,10 +1552,11 @@ ol {
 
 .home-hero {
   position: relative;
-  background-color: #0A0A0A;
+  background-color: transparent !important;
   width: 100%;
   height: 100vh;
-  min-height: 600px;
+  height: 100dvh;
+  min-height: auto;
   overflow: hidden;
   display: flex;
   align-items: center;
@@ -1150,19 +1588,16 @@ ol {
   object-fit: cover;
 }
 
-.hero-video-bg iframe {
+.home-video-fixed-wrapper .hero-video-bg iframe {
   position: absolute;
   top: 50%;
   left: 50%;
   width: 100vw;
   height: 56.25vw;
-  /* 16:9 Aspect Ratio */
   min-height: 100vh;
   min-width: 177.77vh;
-  /* 16:9 Aspect Ratio */
   transform: translate(-50%, -50%);
   pointer-events: none;
-  /* Prevents users from clicking the video */
 }
 
 /* Dark overlay */
@@ -1189,17 +1624,20 @@ ol {
 .home-hero-label {
   font-family: var(--ff-body);
   font-size: var(--fs-label);
-  font-weight: 100;
+  font-weight: 400;
   letter-spacing: 0.5em;
   text-transform: uppercase;
   color: rgba(255, 255, 255, 0.9);
   margin-bottom: var(--space-md);
   opacity: 0;
+}
+
+.home-hero.start-anim .home-hero-label {
   animation: fadeUp 1s var(--ease-out-expo) 0.5s forwards;
 }
 
 .home-hero-title {
-  font-family: var(--ff-display);
+  font-family: 'Cormorant Garamond', var(--ff-display);
   font-size: var(--fs-hero);
   font-weight: 100;
   line-height: 1.1;
@@ -1211,11 +1649,14 @@ ol {
 
 /* Luca Mori Style Additions */
 .luca-mori-title {
-  font-family: 'Ddot Font', "Playfair Display", Georgia, serif;
-  color: #000000 !important;
+  font-family: 'Cormorant Garamond', "Playfair Display", Georgia, serif;
+  color: #ffffff !important;
   text-transform: none !important;
   font-size: clamp(2.5rem, 8vw, 7.5rem);
   white-space: nowrap;
+}
+
+.home-hero.start-anim .luca-mori-title {
   animation-name: lucaFadeOutText !important;
   animation-duration: 2s !important;
   animation-delay: 3.5s !important;
@@ -1224,11 +1665,160 @@ ol {
 }
 
 .luca-mori-title .char {
-  color: #000000 !important;
+  color: #ffffff !important;
+}
+
+/* Bottom-Left Hero Title Override & Style */
+.home-hero-content.bottom-left-hero-content {
+  position: absolute !important;
+  bottom: 8% !important;
+  left: 50% !important;
+  transform: translateX(-50%) !important;
+  text-align: center !important;
+  margin-top: 0 !important;
+  max-width: 900px !important;
+  width: 100% !important;
+  padding: 0 2rem !important;
+}
+
+.home-hero-content.bottom-left-hero-content .luca-mori-title {
+  color: #ffffff !important;
+  text-shadow: 0 2px 12px rgba(0, 0, 0, 0.4) !important;
+  font-size: clamp(2.2rem, 5vw, 4.2rem) !important;
+  font-family: 'Cormorant Garamond', serif !important;
+  font-weight: 300 !important;
+  white-space: normal !important;
+  line-height: 1.1 !important;
+  letter-spacing: normal !important;
+}
+
+.home-hero-content.bottom-left-hero-content .luca-mori-title .char {
+  color: #ffffff !important;
+}
+
+.home-hero-content.bottom-left-hero-content .home-hero-subtitle {
+  color: rgba(239, 233, 215, 0.8) !important;
+  font-family: var(--ff-body) !important;
+  font-weight: 400 !important;
+  text-align: center !important;
+  margin-top: 1.2rem !important;
+  white-space: nowrap !important;
+  font-size: clamp(0.6rem, 0.85vw, 0.85rem) !important;
+  letter-spacing: 0.35em !important;
+  text-transform: uppercase !important;
+  line-height: 1.5 !important;
+  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.6) !important;
+}
+
+.home-hero.start-anim .bottom-left-hero-content .luca-mori-title {
+  animation-name: none !important;
+  /* Disable fade out for bottom-left corner */
+}
+
+/* Sound On/Off Button */
+.hero-sound-toggle {
+  position: absolute;
+  bottom: 40px;
+  right: 40px;
+  z-index: 10;
+  background: rgba(13, 12, 11, 0.45);
+  border: 1px solid rgba(255, 255, 255, 0.25);
+  backdrop-filter: blur(10px);
+  -webkit-backdrop-filter: blur(10px);
+  color: #EFE9D7;
+  width: 44px;
+  height: 44px;
+  border-radius: 50%;
+  cursor: pointer;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  transition: all 0.3s cubic-bezier(0.19, 1, 0.22, 1);
+  padding: 0;
+  outline: none;
+}
+
+.hero-sound-toggle:hover {
+  background: rgba(239, 233, 215, 0.2);
+  border-color: #EFE9D7;
+  transform: scale(1.05);
+}
+
+.hero-sound-toggle svg {
+  width: 18px;
+  height: 18px;
+  stroke: currentColor;
+  fill: none;
+  transition: transform 0.3s ease;
+}
+
+.hero-sound-toggle .icon-unmute {
+  display: none;
+}
+
+.hero-sound-toggle.sound-on .icon-mute {
+  display: none;
+}
+
+.hero-sound-toggle.sound-on .icon-unmute {
+  display: block;
+}
+
+@media (max-width: 768px) {
+  .hero-sound-toggle {
+    bottom: 20px;
+    right: 20px;
+    width: 38px;
+    height: 38px;
+  }
+  .hero-sound-toggle svg {
+    width: 16px;
+    height: 16px;
+  }
+}
+
+/* Force bottom-left positioning on mobile even when .is-mobile vertical centering is active */
+.is-mobile .home-hero-content.bottom-left-hero-content {
+  position: absolute !important;
+  bottom: 10% !important;
+  left: 50% !important;
+  transform: translateX(-50%) !important;
+  width: 100% !important;
+  max-width: 95% !important;
+  height: auto !important;
+  text-align: center !important;
+  margin: 0 !important;
+  padding: 0 1rem !important;
+  display: block !important;
+}
+
+@media (max-width: 767px) {
+  .home-hero-content.bottom-left-hero-content {
+    bottom: 10% !important;
+    left: 50% !important;
+    transform: translateX(-50%) !important;
+    max-width: 95% !important;
+    text-align: center !important;
+  }
+
+  .home-hero-content.bottom-left-hero-content .luca-mori-title {
+    font-size: clamp(1.8rem, 8vw, 3rem) !important;
+    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.5) !important;
+    letter-spacing: normal !important;
+  }
+
+  .home-hero-content.bottom-left-hero-content .home-hero-subtitle {
+    font-size: clamp(0.5rem, 2.2vw, 0.7rem) !important;
+    white-space: nowrap !important;
+    letter-spacing: 0.2em !important;
+    text-align: center !important;
+    line-height: 1.4 !important;
+    text-shadow: 0 2px 6px rgba(0, 0, 0, 0.5) !important;
+  }
 }
 
 .black-font {
-  color: #000000 !important;
+  color: var(--clr-brand-dark) !important;
   font-weight: 600;
 }
 
@@ -1330,6 +1920,9 @@ ol {
 .home-hero-title .char {
   display: inline-block;
   transform: translateY(110%);
+}
+
+.home-hero.start-anim .home-hero-title .char {
   animation: charReveal 0.8s var(--ease-out-expo) forwards;
 }
 
@@ -1340,13 +1933,16 @@ ol {
 }
 
 .home-hero-subtitle {
-  font-family: "Montserrat", Helvetica, Arial, sans-serif;
+  font-family: var(--ff-body);
   font-size: clamp(0.85rem, 1.2vw, 1rem);
-  font-weight: 100;
+  font-weight: 400;
   color: rgba(255, 255, 255, 0.8);
   letter-spacing: 0.25em;
   text-transform: uppercase;
   opacity: 0;
+}
+
+.home-hero.start-anim .home-hero-subtitle {
   animation: fadeUp 1s var(--ease-out-expo) 1.2s forwards;
 }
 
@@ -1354,7 +1950,7 @@ ol {
   display: inline-block;
   font-family: var(--ff-body);
   font-size: var(--fs-small);
-  font-weight: 100;
+  font-weight: 500;
   letter-spacing: 0.2em;
   text-transform: uppercase;
   color: #ffffff;
@@ -1362,10 +1958,13 @@ ol {
   border: 1px solid rgba(255, 255, 255, 0.6);
   transition: all var(--dur-normal) var(--ease-out-expo);
   opacity: 0;
-  animation: fadeUp 1s var(--ease-out-expo) 1.5s forwards;
   margin-top: var(--space-md);
 }
 
+.home-hero.start-anim .home-hero-cta {
+  animation: fadeUp 1s var(--ease-out-expo) 1.5s forwards;
+}
+
 .home-hero-cta:hover {
   background: #ffffff;
   color: var(--clr-text);
@@ -1386,7 +1985,7 @@ ol {
 .ethos-label {
   font-family: var(--ff-body);
   font-size: var(--fs-label);
-  font-weight: 100;
+  font-weight: 500;
   letter-spacing: 0.35em;
   text-transform: uppercase;
   color: var(--clr-text-muted);
@@ -1600,7 +2199,7 @@ ol {
   display: inline-block;
   font-family: var(--ff-body);
   font-size: 13px;
-  font-weight: 100;
+  font-weight: 500;
   letter-spacing: 0.2em;
   text-transform: uppercase;
   color: var(--clr-text);
@@ -1617,185 +2216,340 @@ ol {
   opacity: 1;
 }
 
-.footer {
-  background: var(--clr-bg-footer);
-  color: var(--clr-text-light);
-  padding: var(--space-xl) 0 var(--space-md) 0;
-}
+/* ═══════════════════════════════════════════
 
-.footer-brand {
-  text-align: center;
-  margin-bottom: 2.5rem;
-}
+   CUSTOM FOOTER (WEDDING FILMER STYLE)
 
-.footer-brand-img {
-  height: 60px;
-  width: auto;
-  display: inline-block;
-  filter: invert(1) brightness(2);
+   ═══════════════════════════════════════════ */
+
+.custom-footer {
+  position: relative;
+  background: #ffffff;
+  padding: 0;
+  width: 100%;
+  box-sizing: border-box;
 }
 
-.footer-grid {
-  display: grid;
-  grid-template-columns: 1fr 1fr 1fr;
-  gap: 3rem;
-  padding-bottom: var(--space-lg);
-  border-bottom: 1px solid rgba(255, 255, 255, 0.15);
-  text-align: center;
+.custom-footer-video-wrap {
+  position: relative;
+  height: calc(100vh - 100px);
+  margin: 0 0 15px 0;
+  border-radius: 0 0 35px 35px;
+  overflow: hidden;
+  background: #000000;
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
   align-items: center;
 }
 
-.footer-col:first-child {
-  text-align: left;
+.custom-footer-video-bg {
+  position: absolute;
+  top: 50%;
+  left: 50%;
+  width: 100%;
+  height: 100%;
+  transform: translate(-50%, -50%);
+  pointer-events: none;
+  z-index: 1;
 }
 
-.footer-col:last-child {
-  text-align: right;
+.custom-footer-video-bg iframe {
+  position: absolute;
+  top: 50%;
+  left: 50%;
+  min-width: 100%;
+  min-height: 100%;
+  width: auto;
+  height: auto;
+  aspect-ratio: 16 / 9;
+  transform: translate(-50%, -50%) scale(1.05);
+  opacity: 0.85;
+  pointer-events: none;
+  filter: grayscale(100%) contrast(1.1) brightness(0.9);
 }
 
-.footer-col:nth-child(2) {
-  border-left: 1px solid rgba(255, 255, 255, 0.15);
-  border-right: 1px solid rgba(255, 255, 255, 0.15);
-  padding: 0 1.5rem;
+.custom-footer-controls {
+  display: none !important;
 }
 
-.footer-heading {
-  font-family: var(--ff-display);
-  font-size: 0.9rem;
-  font-weight: 100;
-  letter-spacing: 0.2em;
-  text-transform: uppercase;
-  color: rgba(255, 255, 255, 0.9);
-  margin-bottom: 1.5rem;
+.footer-control-btn {
+  background: transparent;
+  border: none;
+  cursor: pointer;
+  padding: 0;
+  display: flex;
+  justify-content: center;
+  align-items: center;
+  color: rgba(255, 255, 255, 0.6);
+  transition: all 0.3s ease;
+  outline: none;
 }
 
-.footer-links {
-  display: flex;
-  flex-direction: column;
-  gap: 0.75rem;
+.footer-control-btn:hover {
+  color: #ffffff;
+  transform: scale(1.1);
+}
+
+.footer-control-btn svg {
+  width: 18px;
+  height: 18px;
+}
+
+.footer-control-btn.play-pause-btn {
+  width: 46px;
+  height: 46px;
+  background: rgba(255, 255, 255, 0.05);
+  border: 1.5px solid rgba(255, 255, 255, 0.35);
+  border-radius: 50%;
+}
+
+.footer-control-btn.play-pause-btn:hover {
+  background: rgba(255, 255, 255, 0.15);
+  border-color: #ffffff;
+}
+
+.footer-control-btn.play-pause-btn svg {
+  width: 14px;
+  height: 14px;
+}
+
+.footer-control-btn.play-pause-btn .icon-play {
+  display: none;
+  margin-left: 2px; /* optical centering */
+}
+
+.footer-control-btn.play-pause-btn.paused .icon-pause {
+  display: none;
+}
+
+.footer-control-btn.play-pause-btn.paused .icon-play {
+  display: block;
+}
+
+.custom-footer-overlay {
+  position: absolute;
+  inset: 0;
+  background: linear-gradient(180deg, rgba(0, 0, 0, 0.25) 0%, rgba(0, 0, 0, 0.45) 50%, rgba(0, 0, 0, 0.65) 100%);
+  z-index: 2;
 }
 
-.footer-links a {
+.custom-footer-enquire-btn {
+  position: absolute;
+  top: 60%;
+  left: 50%;
+  transform: translate(-50%, -50%);
+  z-index: 3;
+  background: rgba(255, 255, 255, 0.1);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
+  border: 1px solid rgba(255, 255, 255, 0.25);
+  color: #ffffff !important;
+  padding: 14px 32px;
+  border-radius: 50px;
   font-family: var(--ff-body);
-  font-size: 0.9rem;
-  color: rgba(255, 255, 255, 0.7);
-  letter-spacing: 0.05em;
-  transition: color 0.3s ease;
+  font-size: 13px;
+  font-weight: 600;
+  letter-spacing: 0.1em;
+  text-transform: uppercase;
+  text-decoration: none;
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  transition: transform 0.3s cubic-bezier(0.25, 1, 0.5, 1), background-color 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
+  box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.25);
 }
 
-.footer-links a:hover {
-  color: rgba(255, 255, 255, 1);
-  opacity: 1;
+.custom-footer-enquire-btn:hover {
+  transform: translate(-50%, -50%) scale(1.05);
+  background: rgba(255, 255, 255, 0.25);
+  border-color: rgba(255, 255, 255, 0.45);
+  box-shadow: 0 12px 35px rgba(0, 0, 0, 0.35);
+}
+
+.custom-footer-enquire-btn svg {
+  width: 14px;
+  height: 14px;
+  fill: none;
+  stroke: currentColor;
+  stroke-width: 2.5;
+  transition: transform 0.3s ease;
+}
+
+.custom-footer-enquire-btn:hover svg {
+  transform: translateX(4px);
+}
+
+.custom-footer-glass-card {
+  position: absolute;
+  bottom: 125px;
+  left: 40px;
+  right: 40px;
+  z-index: 3;
+  background: rgba(255, 255, 255, 0.05);
+  backdrop-filter: blur(25px);
+  -webkit-backdrop-filter: blur(25px);
+  border: 1px solid rgba(255, 255, 255, 0.12);
+  border-radius: 20px;
+  padding: 25px 45px;
+  display: grid;
+  grid-template-columns: 1fr 2fr 1fr;
+  align-items: center;
+  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.25);
 }
 
-.footer-social-icons {
+.custom-footer-card-col {
   display: flex;
   flex-direction: column;
-  gap: 1rem;
-  align-items: flex-start;
 }
 
-.footer-social-icons a {
-  color: rgba(255, 255, 255, 0.7);
-  font-size: 1.3rem;
-  transition: color 0.3s ease;
+.custom-footer-card-col.left-col {
+  text-align: right;
 }
 
-.footer-social-icons a:hover {
-  color: rgba(255, 255, 255, 1);
-  opacity: 1;
+.custom-footer-card-col.center-col {
+  text-align: center;
+}
+
+.custom-footer-card-col.right-col {
+  text-align: left;
 }
 
-.footer-details p {
+.custom-footer-label {
   font-family: var(--ff-body);
-  font-size: 0.9rem;
-  color: rgba(255, 255, 255, 0.7);
-  line-height: 2;
-  letter-spacing: 0.03em;
+  font-size: 10px;
+  font-weight: 600;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
+  color: rgba(255, 255, 255, 0.5);
+  margin-bottom: 6px;
+}
+
+.custom-footer-value {
+  font-family: var(--ff-body);
+  font-size: 13.5px;
+  font-weight: 500;
+  color: #ffffff;
+  text-decoration: none;
+  letter-spacing: 0.05em;
+  line-height: 1.5;
+  transition: color 0.3s ease;
+}
+
+.custom-footer-value.address-value {
+  font-size: 12.5px;
+  color: rgba(255, 255, 255, 0.95);
 }
 
-.footer-bottom {
+a.custom-footer-value:hover {
+  color: rgba(255, 255, 255, 0.8);
+}
+
+.custom-footer-bottom-bar {
   display: flex;
   justify-content: space-between;
   align-items: center;
-  padding-top: var(--space-md);
+  padding: 25px 40px;
+  background: var(--clr-brand-cream);
+  color: var(--clr-brand-dark);
+  box-sizing: border-box;
+}
+
+.custom-footer-copyright {
   font-family: var(--ff-body);
-  font-size: 0.8rem;
-  color: rgba(255, 255, 255, 0.6);
-  letter-spacing: 0.1em;
+  font-size: 12px;
+  letter-spacing: 0.06em;
+  color: var(--clr-brand-dark);
+  font-weight: 500;
 }
 
-.footer-bottom a {
-  color: rgba(255, 255, 255, 0.8);
-  text-transform: uppercase;
-  letter-spacing: 0.15em;
-  font-size: 0.75rem;
-  transition: color 0.3s ease;
+.custom-footer-copyright a {
+  color: var(--clr-brand-red);
+  text-decoration: none;
+  font-weight: 600;
+  transition: opacity 0.3s ease;
 }
 
-.footer-bottom a:hover {
-  color: #ffffff;
-  opacity: 1;
+.custom-footer-copyright a:hover {
+  opacity: 0.8;
 }
 
-@media (max-width: 768px) {
-  .footer-grid {
-    grid-template-columns: 1fr;
-    text-align: center;
-    gap: 2.5rem;
-  }
+.custom-footer-socials {
+  display: flex;
+  gap: 10px;
+  align-items: center;
+}
 
-  .footer-col:first-child,
-  .footer-col:last-child {
-    text-align: center;
+.custom-footer-social-btn {
+  width: 38px;
+  height: 38px;
+  border-radius: 50%;
+  background: rgba(52, 55, 35, 0.06);
+  color: var(--clr-brand-dark);
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  transition: background-color 0.3s cubic-bezier(0.25, 1, 0.5, 1), transform 0.3s cubic-bezier(0.25, 1, 0.5, 1);
+  text-decoration: none;
+}
+
+.custom-footer-social-btn:hover {
+  background: var(--clr-brand-dark);
+  color: var(--clr-brand-cream);
+  transform: translateY(-2px);
+}
+
+.custom-footer-social-btn svg {
+  width: 15px;
+  height: 15px;
+}
+
+.custom-footer-social-btn svg:not([fill="none"]) {
+  fill: currentColor;
+}
+
+@media (max-width: 1024px) {
+  .custom-footer-glass-card {
+    padding: 20px 30px;
+    left: 30px;
+    right: 30px;
   }
+}
 
-  .footer-col:nth-child(2) {
-    border-left: none;
-    /* Remove left border */
-    border-right: none;
-    /* Remove right border */
-    border-top: 1px solid rgba(255, 255, 255, 0.15);
-    /* Add top border */
-    border-bottom: 1px solid rgba(255, 255, 255, 0.15);
-    /* Add bottom border */
-    padding: 1.5rem 0;
-    /* Reduced vertical padding for mobile */
-  }
-
-  .footer-brand-img {
-    height: 150px;
-    /* Reduced logo height for mobile */
-    max-width: 100%;
-    object-fit: contain;
+@media (max-width: 900px),
+(pointer: coarse) and (max-width: 1200px) {
+  .custom-footer-video-wrap {
+    width: 100%;
+    height: auto;
+    aspect-ratio: 16 / 9;
+    border-radius: 0 0 25px 25px;
   }
 
-  .footer-social-icons {
-    flex-direction: column;
-    justify-content: center;
-    align-items: center;
-    gap: 1.5rem;
+  .custom-footer-glass-card {
+    left: 20px;
+    right: 20px;
+    padding: 15px 20px;
   }
 
-  .footer-bottom {
+  .custom-footer-bottom-bar {
     flex-direction: column;
-    gap: 1rem;
+    gap: 15px;
     text-align: center;
+    padding: 20px 15px;
   }
 }
 
-@media (max-width: 480px) {
-  .footer-brand-img {
-    height: 100px;
-    /* Further reduced height for very small screens */
-  }
-
-  .footer-col:nth-child(2) {
-    padding: 1rem 0;
-    /* Further reduced vertical padding */
-  }
+/* Make custom footer font simple (sans-serif) */
+.custom-footer,
+.custom-footer-enquire-btn,
+.custom-footer-label,
+.custom-footer-value,
+.custom-footer-copyright {
+  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif !important;
 }
 
+
+
 /* ═══════════════════════════════════════════
 
    PAGE HERO — For inner pages
@@ -1840,7 +2594,7 @@ ol {
 .page-hero-label {
   font-family: var(--ff-body);
   font-size: var(--fs-label);
-  font-weight: 100;
+  font-weight: 500;
   letter-spacing: 0.5em;
   text-transform: uppercase;
   color: rgba(255, 255, 255, 0.8);
@@ -1850,7 +2604,7 @@ ol {
 }
 
 .page-hero-title {
-  font-family: var(--ff-display);
+  font-family: 'Cormorant Garamond', var(--ff-display);
   font-size: var(--fs-h1);
   font-weight: 100;
   line-height: 1.2;
@@ -1999,60 +2753,148 @@ ol {
 
    ═══════════════════════════════════════════ */
 
+/* Films Video Hero */
+.films-video-hero {
+  position: relative;
+  height: 50vh;
+  min-height: 350px;
+  width: 100%;
+  overflow: hidden;
+  display: flex;
+  align-items: flex-end;
+  justify-content: flex-start;
+  padding: 0 0 8% 8%;
+}
+
+.films-video-hero .hero-video-bg iframe {
+  position: absolute !important;
+  top: 50% !important;
+  left: 50% !important;
+  transform: translate(-50%, -50%) !important;
+  pointer-events: none !important;
+  width: 177.78vh !important; /* 100vh * 16/9 — ensures video is always wide enough */
+  height: 56.25vw !important; /* 100vw * 9/16 — ensures video is always tall enough */
+  min-width: 100% !important;
+  min-height: 100% !important;
+}
+
+.films-video-hero .hero-video-bg {
+  position: absolute;
+  top: 0;
+  left: 0;
+  width: 100%;
+  height: 100%;
+  z-index: 1;
+}
+
+.films-video-hero .hero-video-overlay {
+  position: absolute;
+  top: 0;
+  left: 0;
+  width: 100%;
+  height: 100%;
+  background: linear-gradient(to bottom, rgba(52, 55, 35, 0.1) 0%, rgba(52, 55, 35, 0.25) 50%, rgba(52, 55, 35, 0.65) 100%);
+  z-index: 2;
+  pointer-events: none;
+}
+
+.films-hero-overlay-content {
+  position: absolute !important;
+  left: 8% !important;
+  bottom: 12% !important;
+  z-index: 3 !important;
+  color: var(--clr-text-light) !important;
+  max-width: 600px !important;
+  text-align: left !important;
+}
+
+.films-hero-headline {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: clamp(2.8rem, 6vw, 4.5rem);
+  font-weight: 100;
+  line-height: 1.2;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
+  color: var(--clr-text-light);
+  margin-bottom: 2.5rem;
+}
+
+.films-hero-btn {
+  display: inline-block;
+  background: transparent;
+  border: 1px solid var(--clr-text-light);
+  color: var(--clr-text-light);
+  font-family: var(--ff-body);
+  font-size: 0.85rem;
+  font-weight: 600;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
+  padding: 0.95rem 2.2rem;
+  border-radius: 50px;
+  cursor: pointer;
+  text-decoration: none;
+  transition: all 0.3s ease;
+}
+
+.films-hero-btn:hover {
+  background: var(--clr-text-light);
+  color: var(--clr-brand-dark);
+}
+
 .films-section {
-  background-color: #e3ddd6;
-  padding: 6rem 0;
-  color: #222222;
+  background-color: var(--clr-brand-cream);
+  padding: 10rem 0;
+  color: var(--clr-text);
 }
 
-/* Category Filters styling */
 .films-filter-container {
   display: flex;
-  justify-content: left;
+  justify-content: center;
   align-items: center;
-  gap: 1rem;
-  margin-bottom: 3.5rem;
   flex-wrap: wrap;
+  gap: 1rem 1.5rem;
+  margin: 0 0 6rem 0;
+  font-family: 'Cormorant Garamond', Georgia, serif;
+  font-size: clamp(1.1rem, 2vw, 1.4rem);
+  text-transform: uppercase;
+  letter-spacing: 0.1em;
 }
 
-.filter-btn {
-  background: transparent;
+.films-filter-container .filter-btn {
+  background: none;
   border: none;
-  font-family: var(--ff-body);
-  font-size: 0.95rem;
-  font-weight: 500;
-  text-transform: none;
-  color: var(--clr-text-muted);
+  color: rgba(52, 55, 35, 0.55);
+  font-family: inherit;
+  font-size: inherit;
+  letter-spacing: inherit;
+  text-transform: inherit;
   cursor: pointer;
-  padding: 0.25rem 0.5rem;
-  transition: color 0.3s ease, border-color 0.3s ease;
-  border-bottom: 1px solid transparent;
-  outline: none;
+  padding: 0.5rem 0.8rem;
+  transition: color 0.3s ease;
 }
 
-.filter-btn:hover {
-  color: var(--clr-text);
+.films-filter-container .filter-btn:hover,
+.films-filter-container .filter-btn.active {
+  color: var(--clr-brand-dark, #343723);
+  font-weight: 500;
 }
 
-.filter-btn.active {
-  color: var(--clr-text);
-  font-weight: 600;
-  border-bottom: 1px solid var(--clr-text);
+.films-filter-container .filter-separator {
+  color: rgba(52, 55, 35, 0.25);
+  font-weight: 300;
+  user-select: none;
 }
 
-.filter-separator {
-  color: rgba(0, 0, 0, 0.15);
-  font-size: 0.85rem;
-  font-weight: 300;
-  pointer-events: none;
+.dynamic-filtered-grid {
+  min-height: 400px;
 }
 
-/* Films Grid styling */
+/* Flexbox list for alternating layout */
 .films-grid {
-  display: grid;
-  grid-template-columns: repeat(3, minmax(0, 1fr));
-  gap: 3.5rem 1.75rem;
-  transition: all 0.5s ease;
+  display: flex;
+  flex-direction: column;
+  gap: 12rem;
+  width: 100%;
 }
 
 /* Individual Film Item styled as an editorial card */
@@ -2064,16 +2906,29 @@ ol {
   outline: none;
   cursor: pointer;
   display: flex;
-  flex-direction: column;
+  flex-direction: row;
+  align-items: center;
+  justify-content: space-between;
+  gap: 6rem;
+  width: 100%;
+  text-decoration: none;
   overflow: visible;
-  transition: opacity 0.4s ease, transform 0.4s ease;
-  transform-origin: center;
+  transition: opacity var(--dur-normal) var(--ease-smooth);
+}
+
+/* Alternating entries */
+.film-card:nth-child(even) {
+  flex-direction: row-reverse;
+}
+
+.film-card:hover {
+  opacity: 0.98;
 }
 
 /* Custom fade animation support for JS filtering */
 .film-card.fade-out {
   opacity: 0;
-  transform: scale(0.95);
+  transform: scale(0.98);
   pointer-events: none;
   position: absolute;
   width: 0;
@@ -2088,46 +2943,48 @@ ol {
   transform: scale(1);
 }
 
-/* Image zoom styling */
+/* Image container */
 .film-card-image {
   position: relative;
-  width: 100%;
+  width: 52%;
   aspect-ratio: 16 / 9;
   overflow: hidden;
-  margin-bottom: 1.25rem;
-  background: #000;
+  background: var(--clr-brand-cream);
+  margin-bottom: 0;
+  flex-shrink: 0;
 }
 
 .film-card-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  transition: transform 0.8s var(--ease-out-expo);
+  transition: transform 1.2s cubic-bezier(0.19, 1, 0.22, 1);
   display: block;
 }
 
 .film-card:hover .film-card-image img,
 .film-card:focus-visible .film-card-image img {
-  transform: scale(1.04);
+  transform: scale(1.03);
 }
 
-/* Play button overlay */
+/* Centered play button overlay */
 .film-play-btn {
   position: absolute;
   top: 50%;
   left: 50%;
   transform: translate(-50%, -50%) scale(0.9);
-  width: 50px;
-  height: 50px;
+  width: 70px;
+  height: 70px;
   border-radius: 50%;
-  background: rgba(255, 255, 255, 0.95);
+  background: rgba(239, 233, 215, 0.9);
+  border: 1px solid rgba(52, 55, 35, 0.1);
   display: flex;
   align-items: center;
   justify-content: center;
   opacity: 0;
-  transition: all 0.4s var(--ease-out-expo);
+  transition: all var(--dur-normal) var(--ease-out-expo);
   pointer-events: none;
-  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
+  z-index: 2;
 }
 
 .film-card:hover .film-play-btn,
@@ -2137,96 +2994,194 @@ ol {
 }
 
 .film-play-btn svg {
-  width: 15px;
-  height: 15px;
-  fill: #222222;
-  margin-left: 2px;
+  width: 18px;
+  height: 18px;
+  fill: var(--clr-brand-dark);
+  margin-left: 3px;
 }
 
-/* Editorial Info styling */
+/* Info container on the other side */
 .film-card-info {
+  width: 40%;
   padding: 0;
-  text-align: left;
+  text-align: center;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  justify-content: center;
+}
+
+.film-card-title {
+  font-family: var(--ff-display);
+  font-size: clamp(2rem, 3.2vw, 3rem);
+  font-weight: 300;
+  text-transform: none;
+  letter-spacing: 0.02em;
+  margin-bottom: 0.75rem;
+  color: var(--clr-text);
+  line-height: 1.25;
+  transition: color var(--dur-fast) ease;
+}
+
+.film-card:hover .film-card-title {
+  color: var(--clr-text-muted);
 }
 
 .film-card-meta {
   display: flex;
+  justify-content: center;
   align-items: center;
   gap: 0.5rem;
   font-family: var(--ff-body);
-  font-size: 0.75rem;
-  letter-spacing: 0.05em;
+  font-size: 0.8rem;
+  font-weight: 500;
+  letter-spacing: 0.1em;
   text-transform: uppercase;
   color: var(--clr-text-muted);
-  margin-bottom: 0.4rem;
+  margin-bottom: 1.5rem;
 }
 
 .meta-dot {
-  color: rgba(0, 0, 0, 0.2);
+  color: rgba(52, 55, 35, 0.3);
   font-size: 0.85rem;
 }
 
-.film-card-title {
-  font-family: var(--ff-display);
-  font-size: 1.3rem;
-  font-weight: 400;
-  text-transform: none;
-  letter-spacing: 0.02em;
-  margin-bottom: 0.4rem;
+.film-card-excerpt {
+  font-family: var(--ff-body);
+  font-size: 0.92rem;
+  line-height: 1.85;
+  color: var(--clr-text-muted);
+  margin-bottom: 2rem;
+  font-weight: normal;
+  text-align: center;
+}
+
+/* Watch Link under the description */
+.film-watch-link {
+  font-family: var(--ff-body);
+  font-size: 0.8rem;
+  font-weight: 600;
+  letter-spacing: 0.2em;
+  text-transform: uppercase;
   color: var(--clr-text);
-  line-height: 1.35;
-  transition: color 0.3s ease;
+  display: inline-block;
+  position: relative;
+  padding-bottom: 4px;
+  cursor: pointer;
+  transition: color var(--dur-fast) ease;
 }
 
-.film-card:hover .film-card-title {
-  color: var(--clr-text-muted);
+.film-watch-link::after {
+  content: '';
+  position: absolute;
+  bottom: 0;
+  left: 50%;
+  width: 40px;
+  height: 1px;
+  background-color: var(--clr-text);
+  transform: translateX(-50%);
+  transition: width var(--dur-normal) var(--ease-out-expo);
 }
 
-.film-card-excerpt {
-  font-family: var(--ff-body);
-  font-size: 0.85rem;
-  line-height: 1.6;
-  color: var(--clr-text-muted);
-  margin-top: 0.25rem;
-  font-weight: normal;
+.film-card:hover .film-watch-link::after {
+  width: 100%;
 }
 
 /* Responsive grid layouts */
-@media (max-width: 1200px) {
+@media (max-width: 1024px) {
   .films-grid {
-    grid-template-columns: repeat(3, minmax(0, 1fr));
-    gap: 3rem 1.5rem;
+    gap: 9rem;
+    padding: 0 2rem;
+  }
+
+  .film-card {
+    gap: 4rem;
+  }
+
+  .film-card-image {
+    width: 50%;
+  }
+
+  .film-card-info {
+    width: 44%;
   }
 }
 
 @media (max-width: 900px) {
+  .films-video-hero {
+    padding: 0 0 12% 5%;
+  }
+
   .films-grid {
-    grid-template-columns: repeat(2, minmax(0, 1fr));
-    gap: 2.5rem 1.25rem;
+    gap: 7rem;
+    padding: 0 2rem;
   }
 
-  .films-filter-container {
-    justify-content: center;
-    margin-bottom: 2.5rem;
+  .film-card,
+  .film-card:nth-child(even) {
+    flex-direction: column;
+    gap: 2.5rem;
+    text-align: center;
+  }
+
+  .film-card-image {
+    width: 100%;
+    aspect-ratio: 16 / 9;
+  }
+
+  .film-card-info {
+    width: 100%;
+    max-width: 650px;
+    margin: 0 auto;
   }
 }
 
 @media (max-width: 600px) {
+  .films-video-hero {
+    padding: 0 0 5% 5%;
+  }
+
+  .films-hero-headline {
+    font-size: clamp(1.2rem, 6vw, 1.8rem);
+    margin-bottom: 1rem;
+  }
+
+  .films-hero-btn {
+    padding: 0.6rem 1.5rem;
+    font-size: 0.75rem;
+  }
+
   .films-grid {
-    grid-template-columns: 1fr;
-    gap: 2.5rem 0;
+    gap: 5rem;
+    padding: 0 1rem;
+  }
+
+  .film-card,
+  .film-card:nth-child(even) {
+    flex-direction: column;
+    gap: 2rem;
+  }
+
+  .film-card-image {
+    width: 100%;
+    aspect-ratio: 16 / 9;
+    margin-bottom: 0;
   }
 
-  .films-section {
-    padding: 4rem 0;
+  .film-card-info {
+    width: 100%;
+    max-width: 100%;
+    padding: 0 0.5rem;
+    text-align: center;
+    margin: 0 auto;
   }
 }
 
 /* ═══════════════ FILM DETAIL SECTION ═══════════════ */
 .film-detail-section {
-  background-color: #e3ddd6;
+  background-color: var(--clr-brand-cream);
   padding: 200px 0 6rem 0;
-  color: #222222;
+  color: var(--clr-text);
 }
 
 /* Elegant Back Button */
@@ -2309,10 +3264,73 @@ ol {
 
 /* Dynamic Suggestions Section */
 .suggestions-section {
-  border-top: 1px solid rgba(0, 0, 0, 0.08);
+  border-top: 1px solid rgba(52, 55, 35, 0.1);
   padding-top: 5rem;
 }
 
+.suggestions-section .films-grid {
+  display: grid;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 3.5rem 1.75rem;
+}
+
+.suggestions-section .film-card {
+  display: flex;
+  flex-direction: column !important;
+  text-align: left;
+  opacity: 1;
+  gap: 0 !important;
+  width: 100%;
+}
+
+.suggestions-section .film-card:nth-child(even) {
+  flex-direction: column !important;
+}
+
+.suggestions-section .film-card-image {
+  width: 100% !important;
+  aspect-ratio: 16 / 9;
+  margin-bottom: 1.25rem;
+  background: #000;
+}
+
+.suggestions-section .film-card-info {
+  width: 100% !important;
+  text-align: left;
+  max-width: none;
+  margin: 0;
+  align-items: flex-start !important;
+}
+
+.suggestions-section .film-card-title {
+  font-family: var(--ff-display);
+  font-size: 1.3rem;
+  font-weight: 400;
+  letter-spacing: 0.02em;
+  margin-bottom: 0.4rem;
+  line-height: 1.35;
+  text-align: left;
+}
+
+.suggestions-section .film-card-meta {
+  display: flex;
+  justify-content: flex-start;
+  align-items: center;
+  gap: 0.5rem;
+  margin-bottom: 0.4rem;
+}
+
+.suggestions-section .film-card-excerpt {
+  font-size: 0.85rem;
+  line-height: 1.6;
+  margin-bottom: 0;
+  text-align: left;
+}
+
+.suggestions-section .film-watch-link {
+  display: none;
+}
+
 .suggestions-title {
   font-family: var(--ff-display);
   font-size: 2.2rem;
@@ -2344,6 +3362,11 @@ ol {
   .suggestions-section {
     padding-top: 3.5rem;
   }
+
+  .suggestions-section .films-grid {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+    gap: 2.5rem 1.25rem;
+  }
 }
 
 @media (max-width: 768px) {
@@ -2359,6 +3382,11 @@ ol {
     height: 38vh;
     margin-bottom: 2.5rem;
   }
+
+  .suggestions-section .films-grid {
+    grid-template-columns: 1fr;
+    gap: 2.5rem 0;
+  }
 }
 
 /* ═══════════════════════════════════════════
@@ -2476,143 +3504,69 @@ ol {
   margin-top: 0.25rem;
 }
 
+
 /* ═══════════════════════════════════════════
 
-   BLOG / JOURNAL PAGE
+   CONTACT SECTION
 
    ═══════════════════════════════════════════ */
 
-.blog-grid {
+.contact-grid {
   display: grid;
-  grid-template-columns: repeat(3, 1fr);
-  gap: 2rem;
+  grid-template-columns: 1fr 1fr;
+  gap: var(--space-xl);
 }
 
-.blog-card {
-  background: var(--clr-bg-warm);
-  overflow: hidden;
-  border: 1px solid var(--clr-border);
-  transition: transform var(--dur-normal) var(--ease-out-expo), box-shadow var(--dur-normal);
+.contact-info {
+  padding-right: 2rem;
 }
 
-.blog-card:hover {
-  transform: translateY(-4px);
-  box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
+.contact-info .section-label {
+  text-align: left;
 }
 
-.blog-card-image {
-  height: 260px;
-  overflow: hidden;
+.contact-info .section-title {
+  text-align: left;
 }
 
-.blog-card-image img {
-  width: 100%;
-  height: 100%;
-  object-fit: cover;
-  transition: transform 0.8s var(--ease-out-expo);
+.contact-details {
+  margin-top: var(--space-md);
+  display: flex;
+  flex-direction: column;
+  gap: var(--space-md);
 }
 
-.blog-card:hover .blog-card-image img {
-  transform: scale(1.05);
+.contact-detail {
+  display: flex;
+  gap: 1rem;
+  align-items: flex-start;
 }
 
-.blog-card-content {
-  padding: 1.75rem;
+.contact-detail-icon {
+  width: 20px;
+  height: 20px;
+  color: var(--clr-text);
+  flex-shrink: 0;
+  margin-top: 3px;
 }
 
-.blog-card-date {
+.contact-detail-label {
   font-size: var(--fs-label);
   letter-spacing: 0.15em;
   text-transform: uppercase;
   color: var(--clr-text-muted);
-  margin-bottom: 0.5rem;
+  margin-bottom: 0.25rem;
 }
 
-.blog-card-title {
-  font-family: var(--ff-display);
-  font-size: 1.1rem;
-  font-weight: 100;
-  margin-bottom: 0.75rem;
-  line-height: 1.4;
-  transition: color var(--dur-fast);
+.contact-detail-value {
+  font-size: var(--fs-body);
+  color: var(--clr-text);
 }
 
-.blog-card:hover .blog-card-title {
-  color: var(--clr-text-muted);
-}
-
-.blog-card-excerpt {
-  font-size: var(--fs-small);
-  color: var(--clr-text-muted);
-  line-height: 1.8;
-  display: -webkit-box;
-  -webkit-line-clamp: 3;
-  -webkit-box-orient: vertical;
-  overflow: hidden;
-}
-
-/* ═══════════════════════════════════════════
-
-   CONTACT SECTION
-
-   ═══════════════════════════════════════════ */
-
-.contact-grid {
-  display: grid;
-  grid-template-columns: 1fr 1fr;
-  gap: var(--space-xl);
-}
-
-.contact-info {
-  padding-right: 2rem;
-}
-
-.contact-info .section-label {
-  text-align: left;
-}
-
-.contact-info .section-title {
-  text-align: left;
-}
-
-.contact-details {
-  margin-top: var(--space-md);
-  display: flex;
-  flex-direction: column;
-  gap: var(--space-md);
-}
-
-.contact-detail {
-  display: flex;
-  gap: 1rem;
-  align-items: flex-start;
-}
-
-.contact-detail-icon {
-  width: 20px;
-  height: 20px;
-  color: var(--clr-text);
-  flex-shrink: 0;
-  margin-top: 3px;
-}
-
-.contact-detail-label {
-  font-size: var(--fs-label);
-  letter-spacing: 0.15em;
-  text-transform: uppercase;
-  color: var(--clr-text-muted);
-  margin-bottom: 0.25rem;
-}
-
-.contact-detail-value {
-  font-size: var(--fs-body);
-  color: var(--clr-text);
-}
-
-.contact-socials {
-  display: flex;
-  gap: 1rem;
-  margin-top: var(--space-lg);
+.contact-socials {
+  display: flex;
+  gap: 1rem;
+  margin-top: var(--space-lg);
 }
 
 .contact-social-link {
@@ -3138,7 +4092,7 @@ ol {
   display: grid;
   grid-template-columns: 1fr 1.25fr 1fr;
   gap: clamp(1.5rem, 3.5vw, 3.5rem);
-  max-width: 1400px;
+  max-width: 87.5rem;
   margin: 0 auto;
   padding: 0 clamp(1.5rem, 4vw, 4rem);
   align-items: start;
@@ -3183,6 +4137,7 @@ ol {
   font-family: var(--ff-display);
   font-style: italic;
   font-weight: normal;
+  color: var(--clr-brand-red);
 }
 
 .modern-column {
@@ -3227,7 +4182,8 @@ ol {
 
 .philosophy-title {
   font-family: var(--ff-body);
-  font-size: var(--fs-label, 10px);
+  font-size: 3rem;
+  font-weight: bold;
   letter-spacing: 0.25em;
   text-transform: uppercase;
   color: var(--clr-text-muted, #888888);
@@ -3267,168 +4223,197 @@ ol {
   pointer-events: none;
 }
 
-/* ---------- Photo Wall Section ---------- */
+/* ---------- Aesthetic Slider Section (Play the Novel Style) ---------- */
 
-.photo-wall-section {
-  padding: 4rem 0;
-  /* Tightened padding to decrease gap */
+.aesthetic-slider-section {
+  padding: 4.5rem 0 6rem 0;
   background-color: var(--clr-bg);
   position: relative;
   z-index: 5;
+  overflow: hidden;
 }
 
-.photo-wall-subheading {
+.aesthetic-slider-header {
   text-align: center;
-  font-family: var(--ff-body);
-  font-size: var(--fs-label, 10px);
-  font-weight: 100;
-  letter-spacing: 0.35em;
-  text-transform: uppercase;
-  color: var(--clr-text-muted);
-  margin-bottom: 2.5rem;
+  margin-bottom: 1.5rem;
   padding: 0 1.5rem;
 }
 
-.iconic-grid-wrapper {
-  max-width: 1400px;
-  margin: 0 auto 2.5rem auto;
-  padding: 0 clamp(1.5rem, 4vw, 4rem);
+.aesthetic-slider-quote {
+  font-family: var(--ff-display);
+  font-size: clamp(1.25rem, 2.5vw, 1.85rem);
+  font-weight: 300;
+  line-height: 1.6;
+  letter-spacing: 0.05em;
+  color: var(--clr-text);
+  max-width: 900px;
+  margin: 0 auto;
+  text-transform: none;
 }
 
-.iconic-grid {
-  display: grid;
-  grid-template-columns: repeat(5, 1fr);
-  grid-template-rows: repeat(3, 230px);
-  gap: 12px;
+.aesthetic-slider-container {
+  position: relative;
   width: 100%;
+  padding: 0 clamp(1.5rem, 4vw, 4rem);
 }
 
-.iconic-cell {
-  position: relative;
+.aesthetic-slider-track {
+  display: flex;
+  gap: 24px;
+  overflow-x: auto;
+  scrollbar-width: none;
+  -webkit-overflow-scrolling: touch;
+  cursor: grab;
+  user-select: none;
+  padding: 10px 0;
+  will-change: scroll-position;
+}
+
+.aesthetic-slider-track::-webkit-scrollbar {
+  display: none;
+}
+
+.aesthetic-slider-track:active {
+  cursor: grabbing;
+}
+
+.aesthetic-slide {
+  flex: 0 0 auto;
+  height: clamp(450px, 30vw, 620px);
+  width: auto;
   overflow: hidden;
-  border-radius: 8px;
+  border-radius: 6px;
   background-color: var(--clr-bg-deep);
+  box-shadow: 0 12px 30px rgba(0, 0, 0, 0.04);
+  transition: transform 0.6s var(--ease-smooth), box-shadow 0.6s var(--ease-smooth);
+  will-change: transform;
 }
 
-.iconic-cell img {
-  width: 100%;
+.aesthetic-slide img {
+  width: auto;
   height: 100%;
-  object-fit: cover;
   display: block;
-  transition: transform 1.2s var(--ease-out-expo), filter 1.2s var(--ease-out-expo);
-  filter: grayscale(10%) contrast(95%);
+  transition: transform 1.2s var(--ease-out-expo);
+  pointer-events: none;
+  will-change: transform;
 }
 
-.iconic-cell:hover img {
-  transform: scale(1.06);
-  filter: grayscale(0%) contrast(100%);
+.aesthetic-slide:hover {
+  transform: translateY(-8px);
+  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
 }
 
-.iconic-text-tile {
-  background-color: #0A0A0A;
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  text-align: center;
-  padding: 1.5rem;
-  border: 1px solid rgba(216, 155, 78, 0.3);
-  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
+.aesthetic-slide:hover img {
+  transform: scale(1.03);
 }
 
-.iconic-text-content {
+.aesthetic-slider-arrow {
+  position: absolute;
+  top: 50%;
+  transform: translateY(-50%);
+  z-index: 10;
+  width: 50px;
+  height: 50px;
+  border-radius: 50%;
+  background: rgba(10, 10, 10, 0.55);
+  backdrop-filter: blur(4px);
+  -webkit-backdrop-filter: blur(4px);
+  border: 1px solid rgba(255, 255, 255, 0.1);
+  color: #ffffff;
   display: flex;
-  flex-direction: column;
   align-items: center;
   justify-content: center;
-  gap: 0.4rem;
+  cursor: pointer;
+  opacity: 1;
+  transition: background var(--dur-fast), transform var(--dur-fast), opacity var(--dur-fast);
 }
 
-.iconic-text-top {
-  font-family: var(--ff-body);
-  font-size: 11px;
-  letter-spacing: 0.15em;
-  text-transform: uppercase;
-  color: rgba(255, 255, 255, 0.7);
+.aesthetic-slider-arrow.arrow-left {
+  left: 2rem;
 }
 
-@keyframes pulseGlow {
-  0% {
-    transform: scale(1);
-    text-shadow: 0 0 18px rgba(216, 155, 78, 0.45);
-  }
+.aesthetic-slider-arrow.arrow-right {
+  right: 2rem;
+}
 
-  50% {
-    transform: scale(1.05);
-    text-shadow: 0 0 30px rgba(216, 155, 78, 0.85);
-  }
+.aesthetic-slider-arrow svg {
+  width: 20px;
+  height: 20px;
+}
 
-  100% {
-    transform: scale(1);
-    text-shadow: 0 0 18px rgba(216, 155, 78, 0.45);
-  }
+.aesthetic-slider-arrow:hover {
+  background: rgba(10, 10, 10, 0.85);
+  transform: translateY(-50%) scale(1.08);
 }
 
-.iconic-text-main {
-  font-family: var(--ff-display);
-  font-size: clamp(1.5rem, 2.4vw, 2.1rem);
-  font-weight: 400;
-  letter-spacing: 0.12em;
-  color: #E8B96A;
-  background: linear-gradient(180deg, #F3D9A8 0%, #D89B4E 100%);
-  -webkit-background-clip: text;
-  background-clip: text;
-  -webkit-text-fill-color: transparent;
-  text-shadow: 0 0 18px rgba(216, 155, 78, 0.45);
+.aesthetic-slider-arrow:active {
+  transform: translateY(-50%) scale(0.95);
+}
+
+.aesthetic-slider-arrow.hidden {
+  opacity: 0;
+  pointer-events: none;
+}
+
+/* ---------- Scroll Heart Line Animation Section ---------- */
+
+.scroll-heart-line-section {
+  padding: 1.5rem 0;
+  background-color: var(--clr-bg);
   position: relative;
-  padding: 0.1em 0.15em;
-  animation: pulseGlow 3s infinite ease-in-out;
+  z-index: 5;
+  overflow: hidden;
+  width: 100%;
 }
 
-.iconic-text-bottom {
-  font-family: var(--ff-body);
-  font-size: 11px;
-  letter-spacing: 0.15em;
-  text-transform: uppercase;
-  color: rgba(255, 255, 255, 0.7);
+.scroll-heart-line-container {
+  width: 100%;
+  max-width: 100%;
+  height: 160px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  padding: 0;
 }
 
-.photo-wall-heading {
-  text-align: center;
-  font-family: var(--ff-display);
-  font-size: clamp(1.8rem, 4vw, 3.2rem);
-  font-weight: 100;
-  letter-spacing: 0.12em;
-  text-transform: uppercase;
-  color: var(--clr-text);
-  margin-top: 0;
-  padding: 0 1.5rem;
+.scroll-heart-line-svg {
+  width: 100%;
+  height: 100%;
+}
+
+.scroll-heart-line-path {
+  stroke-width: 0.6;
+  stroke-dasharray: 1500;
+  stroke-dashoffset: 1500;
+  transition: stroke-dashoffset 0.15s ease-out;
 }
 
-/* Responsiveness for Modern Approach & Iconic Grid */
+/* Responsiveness for Modern Approach & Aesthetic Slider */
 @media (max-width: 1024px) {
-  .modern-tradition-container {
-    grid-template-columns: 1fr 1.2fr;
-    gap: 2rem;
+  .aesthetic-slider-section {
+    padding: 3rem 0 4.5rem 0;
   }
 
-  .modern-tradition-header {
-    grid-column: 1 / span 2;
+  .aesthetic-slider-header {
+    margin-bottom: 1rem;
   }
 
-  .modern-column.col-right {
-    grid-column: 1 / span 2;
-    max-width: 100%;
-    margin-top: 1rem;
+  .aesthetic-slide {
+    height: clamp(380px, 28vw, 480px);
+    width: auto;
   }
 
-  .modern-image-wrapper.right-img {
-    aspect-ratio: 16 / 9;
+  .aesthetic-slider-arrow {
+    width: 44px;
+    height: 44px;
   }
 
-  .iconic-grid {
-    grid-template-columns: repeat(3, 1fr);
-    grid-template-rows: repeat(5, 190px);
-    gap: 8px;
+  .aesthetic-slider-arrow.arrow-left {
+    left: 1.5rem;
+  }
+
+  .aesthetic-slider-arrow.arrow-right {
+    right: 1.5rem;
   }
 }
 
@@ -3437,58 +4422,26 @@ ol {
     padding: 3rem 0;
   }
 
-  .modern-tradition-container {
-    grid-template-columns: 1fr;
-    gap: 1.8rem;
-  }
-
-  .modern-tradition-header {
-    grid-column: 1;
-  }
-
-  .modern-column.col-left,
-  .modern-column.col-right {
-    grid-column: 1;
+  .aesthetic-slider-section {
+    padding: 2rem 0 3.5rem 0;
   }
 
-  .modern-image-wrapper.left-img,
-  .modern-image-wrapper.right-img {
-    aspect-ratio: 4 / 5;
+  .aesthetic-slider-header {
+    margin-bottom: 0.75rem;
   }
 
-  .photo-wall-section {
-    padding: 3rem 0;
-  }
-
-  .iconic-grid {
-    grid-template-columns: none;
-    grid-template-rows: repeat(2, 240px);
-    grid-auto-flow: column;
-    grid-auto-columns: 70vw;
-    gap: 8px;
-    overflow-x: auto;
-    overflow-y: hidden;
-    scroll-snap-type: x mandatory;
-    scrollbar-width: none;
-    -webkit-overflow-scrolling: touch;
+  .aesthetic-slide {
+    height: 350px;
+    width: auto;
+    border-radius: 4px;
   }
 
-  .iconic-grid::-webkit-scrollbar {
+  .aesthetic-slider-arrow {
     display: none;
   }
 
-  .iconic-cell {
-    scroll-snap-align: center;
-    aspect-ratio: auto;
-    height: 100%;
-    width: 100%;
-  }
-
-  .iconic-text-tile {
-    grid-column: auto;
-    grid-row: span 2;
-    aspect-ratio: auto;
-    height: 100%;
+  .aesthetic-slider-container {
+    padding: 0 1.5rem;
   }
 }
 
@@ -3503,8 +4456,9 @@ ol {
 
 
   .home-hero {
-    height: 100vh;
-    min-height: 600px;
+    aspect-ratio: 16 / 9;
+    height: auto;
+    min-height: auto;
   }
 
   .portfolio-grid {
@@ -3604,8 +4558,19 @@ ol {
   }
 
   .home-hero {
-    height: 100vh;
-    min-height: 600px;
+    aspect-ratio: auto;
+    height: 60vh;
+    min-height: 400px;
+  }
+
+  .home-video-fixed-wrapper .hero-video-bg iframe {
+    width: 300% !important;
+    height: 100% !important;
+    min-width: 177.78% !important;
+    min-height: 100% !important;
+    top: 50% !important;
+    left: 50% !important;
+    transform: translate(-50%, -50%) !important;
   }
 
   .hide-mobile {
@@ -3650,20 +4615,27 @@ ol {
   position: relative;
   height: 90vh;
   min-height: 600px;
-  background-image: url('../assets/images/drive_photos/folder_1/PKP_-35.jpg');
-  background-position: center;
-  background-repeat: no-repeat;
-  background-size: cover;
   display: flex;
   align-items: center;
   justify-content: center;
   overflow: hidden;
+  clip-path: inset(0);
 }
 
-@media (min-width: 1025px) {
-  .les-voyages-section {
-    background-attachment: fixed;
-  }
+.les-voyages-section::before {
+  content: "";
+  position: fixed;
+  top: -10%;
+  left: 0;
+  width: 100%;
+  height: 120%;
+  background-image: url('../assets/images/PKP_-35.jpg');
+  background-position: center;
+  background-repeat: no-repeat;
+  background-size: cover;
+  z-index: 0;
+  pointer-events: none;
+  will-change: transform;
 }
 
 .les-voyages-bg-overlay {
@@ -3685,7 +4657,7 @@ ol {
 }
 
 .les-voyages-title {
-  font-family: 'Cinzel Decorative', cursive;
+  font-family: 'Cormorant Garamond', var(--ff-display);
   font-weight: 700;
   color: var(--clr-text-light);
   line-height: 1.2;
@@ -3693,6 +4665,7 @@ ol {
   font-size: clamp(0.8rem, 2.8vw, 2.2rem);
   margin: 0;
   text-transform: uppercase;
+  text-align: center;
   /* Glass effect */
   background: rgba(255, 255, 255, 0.1);
   backdrop-filter: blur(10px);
@@ -3737,7 +4710,7 @@ ol {
 .les-voyages-subtitle {
   font-family: var(--ff-body);
   font-size: clamp(1rem, 1.8vw, 1.35rem);
-  font-weight: 100;
+  font-weight: 400;
   color: rgba(255, 255, 255, 0.85);
   margin-top: 3.5rem;
   max-width: 650px;
@@ -3752,27 +4725,21 @@ ol {
 
 .btn-primary--light:hover {
   background: var(--clr-text-light);
-  color: #000000;
+  color: var(--clr-brand-dark);
   border-color: var(--clr-text-light);
 }
 
 /* Responsive adjustments */
 @media (max-width: 768px) {
   .les-voyages-section {
-    height: 75vh;
-    min-height: 450px;
-  }
-
-  .les-voyages-title.reveal-left {
-    align-self: center;
-    margin-left: 0;
-    transform: translateX(-80px);
+    height: 500px;
+    min-height: auto;
   }
 
-  .les-voyages-title.reveal-right {
-    align-self: center;
-    margin-right: 0;
-    transform: translateX(80px);
+  .les-voyages-title {
+    font-size: clamp(1.4rem, 6vw, 1.8rem);
+    padding: 1.2rem 2rem;
+    background: rgba(255, 255, 255, 0.18);
   }
 
   .les-voyages-subtitle {
@@ -3897,7 +4864,7 @@ ol {
   color: #F2DCAA;
   opacity: 0.5;
   margin-right: 2rem;
-  font-weight: 100;
+  font-weight: 400;
   transition: opacity 0.4s ease;
 }
 
@@ -4065,7 +5032,7 @@ ol {
 }
 
 .fs-contact-title-main {
-  font-family: 'Playfair Display', Georgia, serif;
+  font-family: 'Cormorant Garamond', var(--ff-display);
   font-size: clamp(2.2rem, 5vw, 3.8rem);
   font-weight: 400;
   font-style: italic;
@@ -4076,7 +5043,7 @@ ol {
 }
 
 .fs-contact-title-sub {
-  font-family: 'Poppins', var(--ff-display);
+  font-family: var(--ff-body);
   font-size: clamp(1rem, 2.5vw, 1.35rem);
   font-weight: 400;
   letter-spacing: 0.05em;
@@ -4085,7 +5052,7 @@ ol {
 }
 
 .fs-contact-email {
-  font-family: 'Poppins', var(--ff-body);
+  font-family: var(--ff-body);
   font-size: 1rem;
   font-weight: 400;
   color: var(--clr-text);
@@ -4115,7 +5082,7 @@ ol {
 }
 
 .fs-field-label {
-  font-family: 'Poppins', var(--ff-body);
+  font-family: var(--ff-body);
   font-size: 0.9rem;
   font-weight: 500;
   color: var(--clr-text);
@@ -4137,7 +5104,7 @@ ol {
   background-color: transparent;
   border: 1px solid var(--clr-border-light);
   color: var(--clr-text);
-  font-family: 'Poppins', var(--ff-body);
+  font-family: var(--ff-body);
   font-size: 0.95rem;
   border-radius: 0;
   outline: none;
@@ -4176,149 +5143,186 @@ ol {
 }
 
 .fs-sub-label {
-  font-family: 'Poppins', var(--ff-body);
+  font-family: var(--ff-body);
   font-size: 0.75rem;
   color: var(--clr-text-muted);
 }
 
-.scroll-expand-wrapper {
-  position: relative;
+.cinematic-section {
   width: 100%;
-  height: 250vh;
-  background-color: var(--clr-bg-deep);
+  position: relative;
 }
 
-.scroll-expand-sticky {
-  position: sticky;
-  top: 0;
+/* ──── SOUL + CINEMA BANNER ──── */
+.soul-cinema-container {
+  position: relative;
   width: 100%;
-  height: 100vh;
+  aspect-ratio: 16 / 9; /* Perfect 16:9 ratio as requested */
+  min-height: 500px;
   overflow: hidden;
-  z-index: 1;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  background-color: var(--clr-bg, #EFE9D7); /* Showing cream color under the slit */
 }
 
-.scroll-expand-bg-img {
+.soul-cinema-bg-wrap {
   position: absolute;
   inset: 0;
   width: 100%;
   height: 100%;
-  z-index: 0;
-  pointer-events: none;
-}
-
-.scroll-expand-bg-img img {
-  width: 100%;
-  height: 100%;
-  object-fit: cover;
-  object-position: center;
-}
-
-.scroll-expand-bg-overlay {
-  position: absolute;
-  inset: 0;
-  background: rgba(0, 0, 0, 0.2);
+  /* Shallower asymmetrical clip path to show more video height (matching the second screenshot) */
+  clip-path: polygon(0% 4%, 72% 20%, 100% 2%, 100% 96%, 20% 84%, 0% 97%);
+  background-color: #000;
+  background-image: url('../assets/images/bgvd.jpg');
+  background-size: cover;
+  background-position: center;
+  overflow: hidden;
+  /* Applies black and white aesthetic automatically to fallback image or video iframe */
+  filter: grayscale(100%) contrast(1.1) brightness(0.5);
 }
 
-.scroll-expand-media-box {
+.soul-cinema-video-bg {
   position: absolute;
   top: 50%;
   left: 50%;
-  transform: translate(-50%, -50%);
-  width: 300px;
-  height: 400px;
-  border-radius: 16px;
-  overflow: hidden;
-  z-index: 2;
-  box-shadow: 0px 15px 50px rgba(0, 0, 0, 0.4);
-  will-change: width, height, border-radius, box-shadow;
-}
-
-.scroll-expand-video-wrapper {
-  position: relative;
   width: 100%;
   height: 100%;
+  transform: translate(-50%, -50%);
   pointer-events: none;
 }
 
-.scroll-expand-video-wrapper iframe {
+/* Ensure iframe covers background */
+.soul-cinema-video-bg iframe {
   position: absolute;
   top: 50%;
   left: 50%;
   width: 100vw;
   height: 56.25vw;
-  min-height: 100vh;
+  min-height: 100%;
   min-width: 177.77vh;
   transform: translate(-50%, -50%);
-  pointer-events: none;
 }
 
-.scroll-expand-media-overlay {
+.soul-cinema-overlay {
   position: absolute;
   inset: 0;
-  background: rgba(0, 0, 0, 0.35);
-  mix-blend-mode: multiply;
-  pointer-events: none;
+  background: radial-gradient(circle, rgba(0, 0, 0, 0.1) 0%, rgba(0, 0, 0, 0.4) 100%);
+  z-index: 1;
 }
 
-.scroll-expand-text-container {
-  position: absolute;
-  inset: 0;
+.soul-cinema-text-wrap {
+  position: relative;
+  z-index: 2;
+  text-align: center;
+  color: #ffffff;
+  max-width: 900px;
+  padding: 0 2rem;
   display: flex;
   flex-direction: column;
   align-items: center;
   justify-content: center;
   gap: 1.5rem;
-  z-index: 3;
-  color: var(--clr-text-light, #FAF6F0);
-  pointer-events: none;
-  mix-blend-mode: difference;
 }
 
-.scroll-expand-text-container h2 {
+.soul-cinema-title {
   font-family: var(--ff-display);
-  font-size: clamp(2.5rem, 6vw, 5.5rem);
+  font-size: clamp(2rem, 4vw, 3.2rem);
   font-weight: 300;
+  letter-spacing: 0.25em;
   text-transform: uppercase;
-  letter-spacing: 0.15em;
+  color: #ffffff;
   margin: 0;
-  will-change: transform;
+  line-height: 1.2;
+  text-shadow: 0 4px 15px rgba(0, 0, 0, 0.35);
+  animation: fadeInDown 1.2s var(--ease-out-expo) both;
 }
 
-.scroll-expand-hint-container {
-  position: absolute;
-  bottom: 8%;
-  left: 0;
-  width: 100%;
-  display: flex;
-  justify-content: center;
-  z-index: 4;
-  pointer-events: none;
-  mix-blend-mode: difference;
+/* Styled thin plus sign matching the reference photo */
+.soul-cinema-plus {
+  font-family: var(--ff-body);
+  font-weight: 200;
+  margin: 0 0.8rem;
+  vertical-align: middle;
+  font-size: 0.85em;
+  opacity: 0.85;
 }
 
-.scroll-expand-hint-text {
-  font-family: var(--ff-body);
-  font-size: var(--fs-label);
-  letter-spacing: 0.3em;
-  text-transform: uppercase;
-  color: var(--clr-text-light, #FAF6F0);
-  opacity: 0.7;
+.soul-cinema-description {
+  font-family: 'Playfair Display', Georgia, serif;
+  font-style: italic;
+  font-size: clamp(0.78rem, 0.95vw, 0.95rem);
+  font-weight: 400;
+  line-height: 2.1;
+  color: rgba(255, 255, 255, 0.9);
+  max-width: 720px;
+  margin: 0 auto;
+  text-align: center;
+  letter-spacing: 0.08em;
+  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.25);
+  animation: fadeInUp 1.2s var(--ease-out-expo) 0.2s both;
 }
 
-.scroll-expand-reveal-content {
-  position: relative;
-  width: 100%;
-  z-index: 5;
-  background-color: var(--clr-bg-deep);
-  padding: 6rem 0;
-  opacity: 0;
-  pointer-events: none;
-  transition: opacity 0.5s ease;
+/* Animations */
+@keyframes fadeInDown {
+  from {
+    opacity: 0;
+    transform: translateY(-20px);
+  }
+  to {
+    opacity: 1;
+    transform: translateY(0);
+  }
 }
 
-.scroll-expand-reveal-content.active {
-  opacity: 1;
-  pointer-events: auto;
+@keyframes fadeInUp {
+  from {
+    opacity: 0;
+    transform: translateY(20px);
+  }
+  to {
+    opacity: 1;
+    transform: translateY(0);
+  }
+}
+
+/* Responsive Slanted Breakpoints */
+@media (max-width: 1024px) {
+  .soul-cinema-container {
+    height: auto;
+    aspect-ratio: 16 / 9;
+    min-height: 480px;
+  }
+  .soul-cinema-bg-wrap {
+    clip-path: polygon(0% 4%, 72% 18%, 100% 2%, 100% 96%, 20% 84%, 0% 97%);
+  }
+}
+
+@media (max-width: 768px) {
+  .soul-cinema-container {
+    height: auto;
+    aspect-ratio: 4 / 3;
+    min-height: 400px;
+  }
+  .soul-cinema-bg-wrap {
+    clip-path: polygon(0% 4%, 72% 14%, 100% 2%, 100% 96%, 20% 88%, 0% 98%);
+  }
+  .soul-cinema-text-wrap {
+    max-width: 100%;
+    padding: 0 1.5rem;
+  }
+  .soul-cinema-title {
+    font-size: clamp(1.8rem, 6vw, 2.5rem);
+    letter-spacing: 0.15em;
+  }
+  .soul-cinema-description {
+    font-size: 0.8rem;
+    line-height: 1.8;
+  }
+}
+
+.cinematic-content {
+  padding: 6rem 0;
 }
 
 .scroll-reveal-inner {
@@ -4371,7 +5375,7 @@ ol {
   color: var(--clr-bg);
   border: 1px solid var(--clr-text);
   padding: 16px 20px;
-  font-family: 'Poppins', var(--ff-body);
+  font-family: var(--ff-body);
   font-size: 0.85rem;
   font-weight: 700;
   letter-spacing: 0.2em;
@@ -4424,7 +5428,7 @@ ol {
 }
 
 .fs-feed-label {
-  font-family: 'Poppins', var(--ff-body);
+  font-family: var(--ff-body);
   font-size: 0.75rem;
   font-weight: 500;
   letter-spacing: 0.25em;
@@ -4435,7 +5439,7 @@ ol {
 }
 
 .fs-feed-title {
-  font-family: 'Playfair Display', Georgia, serif;
+  font-family: 'Cormorant Garamond', var(--ff-display);
   font-size: clamp(1.6rem, 3.5vw, 2.6rem);
   font-weight: 400;
   color: var(--clr-text);
@@ -4444,7 +5448,7 @@ ol {
 
 .fs-feed-grid {
   display: grid;
-  grid-template-columns: repeat(6, 1fr);
+  grid-template-columns: repeat(5, 1fr);
   gap: 12px;
   padding: 0 40px;
   margin-top: var(--space-md);
@@ -4510,7 +5514,7 @@ ol {
 
 .fs-feed-tag {
   color: rgba(250, 246, 240, 0.6);
-  font-family: 'Poppins', var(--ff-body);
+  font-family: var(--ff-body);
   font-size: 0.75rem;
   font-weight: 500;
   letter-spacing: 0.05em;
@@ -4527,7 +5531,7 @@ ol {
 
 @media (max-width: 768px) {
   .fs-contact-section {
-    padding: var(--space-lg) 0;
+    padding: 1.5rem 0 var(--space-lg);
   }
 
   .fs-instagram-feed {
@@ -4634,7 +5638,7 @@ ol {
   color: #F2DCAA;
   margin-bottom: 0.8rem;
   text-transform: none;
-  font-weight: 100;
+  font-weight: 400;
 }
 
 .wildly-free-heading {
@@ -4648,7 +5652,7 @@ ol {
 }
 
 .wildly-free-heading .highlight-italic {
-  font-family: 'Playfair Display', Georgia, serif;
+  font-family: 'Cormorant Garamond', var(--ff-display);
   font-style: italic;
   text-transform: none;
   color: #F2DCAA;
@@ -5167,7 +6171,7 @@ ol {
 }
 
 .portfolio-slider-section .story-desc-text {
-  font-family: 'Montserrat', sans-serif;
+  font-family: var(--ff-body);
   font-size: clamp(0.85rem, 1.1vw, 1rem);
   font-weight: 400;
   line-height: 1.7;
@@ -5413,7 +6417,7 @@ ol {
   font-family: var(--ff-display);
   font-size: clamp(2.8rem, 7vw, 6rem);
   font-weight: 100;
-  color: #FAF6F0;
+  color: #ffffff;
   line-height: 1;
   letter-spacing: -0.02em;
   text-transform: uppercase;
@@ -5698,270 +6702,356 @@ ol {
   transform: scale(1.05);
 }
 
-/* Responsiveness: on small screens, stack them or show as standard grid/scroll */
-@media (max-width: 768px) {
+/* Responsiveness: keep the premium horizontal accordion layout even on mobile/tablet screens */
+@media (max-width: 900px),
+(pointer: coarse) and (max-width: 1200px) {
   .creations-gallery-container {
-    height: auto;
-    flex-direction: column;
-    gap: 12px;
+    height: 300px;
+    flex-direction: row;
+    gap: 6px;
+    padding: 0 1rem;
   }
 
   .creations-gallery-item {
-    width: 100%;
-    height: 200px;
-    flex-grow: 0;
+    width: 40px;
+    height: 100%;
+    flex-grow: 1;
   }
 
   .creations-gallery-item:hover {
-    flex-grow: 0;
+    flex-grow: 8;
   }
 }
 
 /* ═══════════════════════════════════════════
-   PORTFOLIO REDESIGN (HOUSE ON THE CLOUDS STYLE)
+   STORIES PAGE (NAMAN VERMA STYLE)
    ═══════════════════════════════════════════ */
 
 body.portfolio-body {
-  background-color: var(--clr-bg);
+  background-color: #f5f2eb;
+  /* Warm cream/beige background matching Naman Verma */
 }
 
-.portfolio-spacer {
-  height: 180px;
-}
-
-.portfolio-white-container {
-  background-color: #ffffff;
-  border-radius: 40px;
-  padding: clamp(3rem, 6vw, 6rem) clamp(2rem, 5vw, 5rem);
-  margin: 0 clamp(1.5rem, 5vw, 5rem) 5rem clamp(1.5rem, 5vw, 5rem);
-  box-shadow: 0 30px 70px rgba(0, 0, 0, 0.02);
+.stories-spacer {
+  display: none;
 }
 
-.portfolio-title-new {
-  font-family: var(--ff-display);
-  font-size: clamp(2rem, 5vw, 3.2rem);
-  font-weight: 300;
-  color: #1a1917;
-  line-height: 1.2;
-  margin-bottom: 2.5rem;
-  text-transform: uppercase;
-  letter-spacing: -0.01em;
-  text-align: center;
+.stories-section {
+  padding: 11.25rem 0 8rem 0;
+  background-color: #f5f2eb;
 }
 
-.portfolio-filter-bar {
+.stories-feed {
+  max-width: 68.75rem;
+  margin: 0 auto;
+  padding: 0 1.875rem;
   display: flex;
-  justify-content: center;
-  align-items: center;
-  gap: 0.5rem;
-  margin-bottom: 4rem;
-  font-family: var(--ff-body);
-  font-size: 13px;
-  letter-spacing: 0.05em;
-  color: #888888;
-  text-transform: uppercase;
+  flex-direction: column;
+  gap: 6.25rem;
+  /* Generous gap between entries */
 }
 
-.portfolio-filter-btn {
-  background: none;
-  border: none;
-  padding: 0 0.5rem;
-  font-family: var(--ff-body);
-  font-size: 13px;
-  letter-spacing: 0.05em;
-  color: #888888;
-  cursor: pointer;
-  transition: color 0.3s ease, border-bottom-color 0.3s ease;
-  border-bottom: 1px solid transparent;
-  text-transform: uppercase;
-}
+@media (min-width: 1025px) {
+  .stories-section {
+    padding: 10rem 0 8rem 0;
+  }
 
-.portfolio-filter-btn:hover {
-  color: #1a1917;
-}
+  .stories-feed {
+    max-width: 68.75rem;
+    margin: 0 auto;
+    padding: 0 1.875rem;
+    display: flex;
+    flex-direction: column;
+    gap: 6.25rem;
+  }
 
-.portfolio-filter-btn.active {
-  color: #1a1917;
-  font-weight: 600;
-  border-bottom: 1px solid #1a1917;
-}
+  .story-entry {
+    margin: 0;
+    border: none;
+    background-color: transparent;
+    box-shadow: none;
+    border-radius: 0;
+  }
 
-.filter-separator {
-  color: #dddddd;
-  user-select: none;
-}
+  .story-entry-body {
+    padding: 1.875rem 0;
+  }
 
-.portfolio-grid-new {
-  display: grid;
-  grid-template-columns: repeat(4, 1fr);
-  gap: 2.5rem 2rem;
+  .story-entry-image {
+    aspect-ratio: 2.2 / 1;
+  }
 }
 
-.portfolio-card {
+/* ── Individual Story Entry ── */
+
+.story-entry {
   display: flex;
   flex-direction: column;
-  cursor: pointer;
+  width: 100%;
   text-decoration: none;
-  color: inherit;
+  color: #1a1917;
+  cursor: pointer;
+  position: relative;
   opacity: 0;
-  transform: translateY(40px) scale(0.98);
-  transition: opacity 0.8s cubic-bezier(0.19, 1, 0.22, 1), transform 0.8s cubic-bezier(0.19, 1, 0.22, 1);
-  perspective: 1000px; /* Enable 3D perspective space for the card context */
+  transform: translateY(40px);
+  transition: opacity 1s cubic-bezier(0.19, 1, 0.22, 1),
+    transform 1s cubic-bezier(0.19, 1, 0.22, 1);
 }
 
-.portfolio-card.revealed {
+.story-entry.revealed {
   opacity: 1;
-  transform: translateY(0) scale(1);
+  transform: translateY(0);
 }
 
-.portfolio-card.hidden {
-  opacity: 0;
-  transform: scale(0.95);
-  pointer-events: none;
-  position: absolute;
-  width: 0;
-  height: 0;
-  overflow: hidden;
-  margin: 0;
-  padding: 0;
-  border: none;
-}
+/* ── Widescreen Image Container ── */
 
-.portfolio-card-img-wrapper {
+.story-entry-image {
   position: relative;
   width: 100%;
-  aspect-ratio: 3 / 4;
+  aspect-ratio: 2.2 / 1;
   overflow: hidden;
-  border-radius: 4px;
-  margin-bottom: 1.5rem;
-  background-color: #f7f7f7;
-  transform-style: preserve-3d; /* Preserve 3D context for children */
-  will-change: transform;
-  transition: transform 0.5s cubic-bezier(0.25, 1, 0.5, 1); /* Smooth snapback transition */
-}
-
-.portfolio-card-sheen {
-  position: absolute;
-  inset: 0;
-  background: radial-gradient(circle at var(--sheen-x, 50%) var(--sheen-y, 50%), rgba(255, 255, 255, 0.12) 0%, transparent 60%);
-  pointer-events: none;
-  z-index: 2;
-  opacity: 0;
-  transition: opacity 0.3s ease;
-  mix-blend-mode: overlay;
-  transform: translateZ(20px); /* Lift sheen forward in 3D space */
+  background-color: #e4dfd5;
 }
 
-.portfolio-card:hover .portfolio-card-sheen {
-  opacity: 1;
-}
-
-.portfolio-card-img-wrapper img {
+.story-entry-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
   display: block;
-  transform: translateZ(0); /* Anchor image at base of 3D stack */
-  transition: transform 0.6s cubic-bezier(0.25, 1, 0.5, 1);
+  transition: transform 1.5s cubic-bezier(0.19, 1, 0.22, 1);
 }
 
-.portfolio-card:hover .portfolio-card-img-wrapper img {
-  transform: translateZ(10px) scale(1.03); /* Pop image forward slightly on hover */
+.story-entry:hover .story-entry-image img {
+  transform: scale(1.025);
 }
 
-.portfolio-card-title {
-  font-family: var(--ff-display);
-  font-size: clamp(1.4rem, 2vw, 1.8rem);
-  font-weight: 300;
-  line-height: 1.25;
-  margin-bottom: 1rem;
-  color: #1a1917;
-  transition: color 0.3s ease;
+/* Subtle overlay on the top-left of the image to ensure text legibility */
+.story-entry-image::after {
+  content: '';
+  position: absolute;
+  top: 0;
+  left: 0;
+  right: 0;
+  bottom: 0;
+  background: linear-gradient(135deg, rgba(0, 0, 0, 0.2) 0%, rgba(0, 0, 0, 0) 50%);
+  pointer-events: none;
+  z-index: 1;
 }
 
-.portfolio-card:hover .portfolio-card-title {
-  color: #7D2826; /* Highlights in Foreshadow wine red on hover */
-}
+/* ── Overlaid Serif Title ── */
 
-.portfolio-card-excerpt {
-  font-family: var(--ff-body);
-  font-size: 13px;
-  line-height: 1.6;
-  color: #555555;
-  margin-bottom: 1.5rem;
-  display: -webkit-box;
-  -webkit-line-clamp: 5;
-  -webkit-box-orient: vertical;
-  overflow: hidden;
+.story-entry-image-title {
+  position: absolute;
+  top: clamp(20px, 3.5vw, 40px);
+  left: clamp(20px, 3.5vw, 40px);
+  color: #ffffff;
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: clamp(1.4rem, 3.2vw, 2.8rem);
   font-weight: 300;
+  text-transform: uppercase;
+  letter-spacing: 0.04em;
+  margin: 0;
+  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.15);
+  pointer-events: none;
+  z-index: 2;
 }
 
-.portfolio-card-footer {
-  margin-top: auto;
+/* ── Story Body below image ── */
+
+.story-entry-body {
+  padding-top: 25px;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-start;
+  width: 100%;
+}
+
+.story-entry-title {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: clamp(1.3rem, 2.2vw, 1.9rem);
+  font-weight: 400;
+  text-transform: uppercase;
+  letter-spacing: 0.12em;
+  color: #1a1917;
+  margin: 0 0 8px 0;
+  line-height: 1.2;
+  transition: color 0.3s ease;
+}
+
+.story-entry:hover .story-entry-title {
+  color: #6d665b;
+}
+
+.story-entry-date {
+  font-family: var(--ff-body);
+  font-size: 11px;
+  font-weight: 400;
+  color: #8c857b;
+  letter-spacing: 0.05em;
+  margin-bottom: 20px;
+}
+
+.story-entry-divider {
+  width: 30px;
+  height: 1px;
+  background-color: #d1cabe;
+  margin-bottom: 18px;
+}
+
+.story-entry-readmore {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 13px;
+  font-style: italic;
+  color: #1a1917;
+  text-transform: capitalize;
+  margin-bottom: 30px;
+  position: relative;
+  display: inline-block;
+  padding-bottom: 2px;
+}
+
+.story-entry-readmore::after {
+  content: '';
+  position: absolute;
+  bottom: 0;
+  left: 0;
+  width: 100%;
+  height: 1px;
+  background-color: #1a1917;
+  transform: scaleX(0);
+  transform-origin: left;
+  transition: transform 0.4s cubic-bezier(0.19, 1, 0.22, 1);
+}
+
+.story-entry:hover .story-entry-readmore::after {
+  transform: scaleX(1);
+}
+
+/* ── Meta row with comments, share and likes ── */
+
+.story-entry-meta-row {
+  width: 100%;
   display: flex;
   justify-content: space-between;
   align-items: center;
-  border-top: 1px solid #f0f0f0;
-  padding-top: 1.25rem;
+  padding-top: 18px;
+  border-top: 1px solid rgba(26, 25, 23, 0.08);
   font-family: var(--ff-body);
   font-size: 11px;
+  color: #8c857b;
   letter-spacing: 0.05em;
-  text-transform: uppercase;
 }
 
-.portfolio-card-more {
+.story-entry-meta-left {
+  display: flex;
+  align-items: center;
+  gap: 20px;
+}
+
+.story-entry-comments,
+.story-entry-share {
+  transition: color 0.3s ease;
+}
+
+.story-entry-share:hover,
+.story-entry-comments:hover {
   color: #1a1917;
-  font-weight: 600;
-  transition: opacity 0.3s ease;
 }
 
-.portfolio-card:hover .portfolio-card-more {
-  opacity: 0.7;
+.story-entry-meta-right {
+  display: flex;
+  align-items: center;
 }
 
-.portfolio-card-category {
-  color: #888888;
+.story-entry-likes {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  transition: color 0.3s ease;
 }
 
-@media (max-width: 1200px) {
-  .portfolio-grid-new {
-    grid-template-columns: repeat(2, 1fr);
-    gap: 3rem 2rem;
-  }
+.story-entry-likes svg.heart-icon {
+  fill: #8c857b;
+  transition: transform 0.3s ease, fill 0.3s ease;
+}
+
+.story-entry:hover .story-entry-likes svg.heart-icon {
+  transform: scale(1.15);
+  fill: #c72c2c;
+}
+
+.story-entry:hover .story-entry-likes {
+  color: #1a1917;
+}
+
+.story-entry-likes.liked svg.heart-icon {
+  fill: #c72c2c !important;
+}
+
+.story-entry-likes.liked {
+  color: #1a1917 !important;
+  cursor: default;
 }
 
+/* ── Responsive styling adjustments ── */
+
 @media (max-width: 1024px) {
-  .portfolio-spacer {
+  .stories-spacer {
     height: 120px;
   }
-  .portfolio-white-container {
-    border-radius: 30px;
-    padding: 3rem 2.5rem;
-    margin: 0 2rem 3rem 2rem;
+
+  .stories-feed {
+    padding: 0 24px;
+    gap: 80px;
   }
 }
 
 @media (max-width: 768px) {
-  .portfolio-spacer {
+  .stories-spacer {
     height: 100px;
   }
-  .portfolio-white-container {
-    border-radius: 20px;
-    padding: 2.5rem 1.5rem;
-    margin: 0 1rem 2rem 1rem;
+
+  .stories-feed {
+    gap: 60px;
   }
-  .portfolio-grid-new {
-    grid-template-columns: 1fr;
-    gap: 2.5rem;
+
+  .story-entry-image {
+    aspect-ratio: 1.8 / 1;
   }
-  .portfolio-filter-bar {
-    margin-bottom: 2.5rem;
+
+  .story-entry-body {
+    padding-top: 20px;
+  }
+}
+
+@media (max-width: 480px) {
+  .story-entry-image {
+    aspect-ratio: 1.5 / 1;
+  }
+
+  .story-entry-image-title {
+    top: 20px;
+    left: 20px;
+  }
+
+  .story-entry-date {
+    margin-bottom: 15px;
+  }
+
+  .story-entry-divider {
+    margin-bottom: 15px;
+  }
+
+  .story-entry-readmore {
+    margin-bottom: 25px;
   }
-  .portfolio-card-excerpt {
-    -webkit-line-clamp: 4;
+
+  .story-entry-meta-row {
+    padding-top: 15px;
   }
 }
 
+
 /* ═══════════════════════════════════════════
    GALLERY DETAIL PAGE REDESIGN (EDITORIAL)
    ═══════════════════════════════════════════ */
@@ -6035,49 +7125,112 @@ body.gallery-body {
   min-height: 40vh;
 }
 
-.gallery-page-grid-new {
-  display: flex;
-  flex-wrap: wrap;
-  max-width: 1100px;
+/* ---------- Gallery Intro Section ---------- */
+.gallery-intro-section {
+  padding: 6rem 1.5rem 3rem 1.5rem;
+  background-color: transparent;
+}
+
+.gallery-intro-container {
+  max-width: 800px;
   margin: 0 auto;
-  gap: 30px; /* Vertical gap between rows */
-  width: 100%;
+  text-align: center;
 }
 
-/* Row-based Layout Containers */
-.gallery-row-full {
-  width: 100%;
+.gallery-desc-new {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: clamp(1.1rem, 1.5vw, 1.3rem);
+  font-style: italic;
+  line-height: 1.8;
+  color: #555555;
+  margin-bottom: 2rem;
 }
 
-.gallery-row-split {
-  display: flex;
-  width: 100%;
-  gap: 30px; /* Horizontal gap between split photos */
+.gallery-intro-divider {
+  width: 60px;
+  height: 1px;
+  background-color: rgba(26, 25, 23, 0.12);
+  margin: 0 auto;
+}
+
+/* ---------- Gallery Scroll Indicator ---------- */
+.gallery-hero-scroll-indicator {
+  position: absolute;
+  bottom: 40px;
+  left: 50%;
+  transform: translateX(-50%);
+  z-index: 5;
+  cursor: pointer;
+  animation: heroBounce 2s infinite ease-in-out;
+  transition: opacity 0.3s ease;
+  opacity: 0.6;
 }
 
-.gallery-row-split .gallery-page-item-new {
-  width: calc(50% - 15px);
+.gallery-hero-scroll-indicator:hover {
+  opacity: 1;
 }
 
-.gallery-row-triple {
-  display: flex;
+.gallery-hero-scroll-indicator svg {
+  width: 24px;
+  height: 24px;
+  stroke: #FAF6F0;
+}
+
+@keyframes heroBounce {
+
+  0%,
+  20%,
+  50%,
+  80%,
+  100% {
+    transform: translateX(-50%) translateY(0);
+  }
+
+  40% {
+    transform: translateX(-50%) translateY(-10px);
+  }
+
+  60% {
+    transform: translateX(-50%) translateY(-5px);
+  }
+}
+
+/* ---------- Refactored Gallery Grid (Kavya & Karan Grid Layout) ---------- */
+/* ---------- Responsive Masonry Gallery Grid ---------- */
+.gallery-page-grid-new {
+  display: flex !important;
+  flex-direction: row !important;
+  justify-content: center;
+  align-items: flex-start;
   width: 100%;
-  gap: 30px; /* Horizontal gap between triple photos */
+  max-width: 1400px;
+  margin: 0 auto;
+  gap: 16px;
+  content-visibility: auto;
+  /* Optimizes rendering for long lists of images */
 }
 
-.gallery-row-triple .gallery-page-item-new {
-  width: calc(33.333% - 20px);
+.gallery-col {
+  display: flex;
+  flex-direction: column;
+  flex: 1;
+  gap: 16px;
+  min-width: 0;
 }
 
+/* Item elements */
 .gallery-page-item-new {
+  display: block;
   position: relative;
-  border-radius: 4px;
+  border-radius: 12px;
   overflow: hidden;
-  cursor: pointer;
+  cursor: default;
   opacity: 0;
   transform: translateY(30px);
-  transition: opacity 0.8s ease, transform 0.8s cubic-bezier(0.19, 1, 0.22, 1);
-  will-change: transform, opacity;
+  transition: opacity 0.8s cubic-bezier(0.19, 1, 0.22, 1), 
+              transform 0.8s cubic-bezier(0.19, 1, 0.22, 1), 
+              box-shadow 0.3s ease;
+  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.03);
 }
 
 .gallery-page-item-new.gallery-item-revealed {
@@ -6085,45 +7238,2758 @@ body.gallery-body {
   transform: translateY(0);
 }
 
+.gallery-page-item-new:hover {
+  transform: translateY(-4px) scale(1.02);
+  box-shadow: 0 12px 30px rgba(0, 0, 0, 0.12);
+}
+
 .gallery-page-item-new img {
   width: 100%;
   height: auto;
   display: block;
-  transition: transform 0.8s var(--ease-out-expo);
+  border-radius: 12px;
+  object-fit: contain; /* Do not crop, stretch or zoom */
+  transition: transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
+}
+
+/* Naman Verma Stacked Gallery Style (for folder_5) */
+.gallery-naman-style {
+  max-width: 1000px !important;
+  width: 100% !important;
+  display: flex !important;
+  flex-direction: column !important;
+  align-items: center !important;
+  gap: 5px !important;
+  margin: 0 auto !important;
+  padding: 0 !important;
 }
 
-.gallery-page-item-new:hover img {
-  transform: scale(1.025);
+/* Each row is a flex container */
+.gallery-naman-style .gallery-row {
+  display: flex !important;
+  flex-direction: row !important;
+  width: 100% !important;
+  gap: 5px !important;
 }
 
-@media (max-width: 1024px) {
-  .gallery-spacer {
-    height: 120px;
-  }
+/* Items inside rows */
+.gallery-naman-style .gallery-row .gallery-page-item-new {
+  flex: 1 !important;
+  min-width: 0 !important;
+  margin: 0 !important;
+  border-radius: 0 !important;
+  box-shadow: none !important;
+  overflow: hidden !important;
+  transform: translateY(20px) !important;
+  transition: opacity 0.6s ease, transform 0.6s ease !important;
+}
+
+.gallery-naman-style .gallery-row .gallery-page-item-new.gallery-item-revealed {
+  opacity: 1 !important;
+  transform: translateY(0) !important;
+}
+
+.gallery-naman-style .gallery-row .gallery-page-item-new:hover {
+  transform: none !important;
+  box-shadow: none !important;
+}
+
+/* Images fill their container without cropping */
+.gallery-naman-style .gallery-page-item-new img {
+  width: 100% !important;
+  height: auto !important;
+  border-radius: 0 !important;
+  display: block !important;
 }
 
+/* Single landscape image row - no max-width constraint needed, takes full row */
+
+/* Center and constrain single portrait images in a row so they don't stretch too wide */
+.gallery-naman-style .gallery-row:has(> .gallery-page-item-new[data-orientation="portrait"]:only-child) {
+  justify-content: center !important;
+}
+
+.gallery-naman-style .gallery-row > .gallery-page-item-new[data-orientation="portrait"]:only-child {
+  max-width: 600px !important;
+  width: 100% !important;
+}
+
+/* Mobile: keep 2 photos per row */
 @media (max-width: 768px) {
-  .gallery-spacer {
-    height: 100px;
-  }
-  .gallery-header-content-new {
-    margin-bottom: 2.5rem;
+  .gallery-naman-style {
+    gap: 4px !important;
   }
-  .gallery-page-grid-new {
-    gap: 15px; /* Smaller gaps on mobile */
+  .gallery-naman-style .gallery-row {
+    flex-direction: row !important;
+    gap: 4px !important;
   }
-  .gallery-row-split {
-    flex-direction: column;
-    gap: 15px;
-  }
-  .gallery-row-split .gallery-page-item-new {
-    width: 100%;
-  }
-  .gallery-row-triple {
-    flex-direction: column;
-    gap: 15px;
+}
+
+@media (max-width: 768px) {
+  .gallery-intro-section {
+    padding: 4rem 1rem 2rem 1rem;
   }
-  .gallery-row-triple .gallery-page-item-new {
-    width: 100%;
+
+  .gallery-desc-new {
+    font-size: 1.05rem;
+    line-height: 1.7;
   }
-}
\ No newline at end of file
+}
+
+
+
+/* ---------- Contact Page Views Counter ---------- */
+.contact-views-container {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  margin-top: 25px;
+  padding: 8px 16px;
+  background-color: rgba(26, 25, 23, 0.03);
+  border: 1px solid rgba(26, 25, 23, 0.08);
+  border-radius: 30px;
+  font-family: var(--ff-body);
+  font-size: 11px;
+  color: #8c857b;
+  letter-spacing: 0.08em;
+}
+
+.contact-views-label {
+  font-weight: 400;
+  text-transform: uppercase;
+}
+
+.contact-views-count {
+  font-weight: 700;
+  color: #1a1917;
+}
+
+/* ---------- Gallery Page Engagement Section ---------- */
+.gallery-engagement-section {
+  background: transparent;
+  padding: 2rem 0 6rem 0;
+  max-width: 1100px;
+  margin: 0 auto;
+}
+
+.gallery-engagement-container {
+  padding: 0 30px;
+}
+
+.gallery-engagement-section .story-entry-meta-row {
+  border-top: 1px solid rgba(26, 25, 23, 0.08);
+  padding-top: 25px;
+}
+
+/* ---------- Detailed Story Comment Section ---------- */
+.gallery-comments-section {
+  margin-top: 40px;
+  width: 100%;
+}
+
+.comments-section-title {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 1.5rem;
+  font-weight: 400;
+  text-transform: uppercase;
+  letter-spacing: 0.1em;
+  color: #1a1917;
+  margin-bottom: 25px;
+  border-bottom: 1px solid rgba(26, 25, 23, 0.08);
+  padding-bottom: 10px;
+}
+
+.comments-list {
+  display: flex;
+  flex-direction: column;
+  gap: 20px;
+  margin-bottom: 40px;
+}
+
+.comment-item {
+  display: flex;
+  gap: 15px;
+  padding: 15px 0;
+  border-bottom: 1px dashed rgba(26, 25, 23, 0.05);
+}
+
+.comment-avatar {
+  width: 40px;
+  height: 40px;
+  border-radius: 50%;
+  background-color: rgba(26, 25, 23, 0.05);
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  font-family: var(--ff-body);
+  font-weight: 600;
+  font-size: 14px;
+  color: #8c857b;
+  text-transform: uppercase;
+}
+
+.comment-content {
+  flex-grow: 1;
+}
+
+.comment-header {
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  margin-bottom: 5px;
+}
+
+.comment-author {
+  font-family: var(--ff-body);
+  font-weight: 600;
+  font-size: 13px;
+  color: #1a1917;
+}
+
+.comment-date {
+  font-family: var(--ff-body);
+  font-size: 10px;
+  color: #8c857b;
+}
+
+.comment-body {
+  font-family: var(--ff-body);
+  font-size: 14px;
+  line-height: 1.6;
+  color: #555555;
+  margin: 0;
+}
+
+/* Comment Form */
+.comment-form {
+  background-color: rgba(26, 25, 23, 0.02);
+  border: 1px solid rgba(26, 25, 23, 0.05);
+  padding: 30px;
+  border-radius: 4px;
+}
+
+.comment-form-title {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 1.2rem;
+  font-style: italic;
+  margin-bottom: 20px;
+  color: #1a1917;
+}
+
+.comment-form-row {
+  display: grid;
+  grid-template-columns: 1fr 1fr;
+  gap: 20px;
+  margin-bottom: 20px;
+}
+
+.comment-form-group {
+  margin-bottom: 15px;
+}
+
+.comment-form-group input,
+.comment-form-group textarea {
+  width: 100%;
+  padding: 12px 15px;
+  background-color: #ffffff;
+  border: 1px solid rgba(26, 25, 23, 0.12);
+  border-radius: 2px;
+  font-family: var(--ff-body);
+  font-size: 13px;
+  color: #1a1917;
+  transition: border-color 0.3s ease;
+}
+
+.comment-form-group input:focus,
+.comment-form-group textarea:focus {
+  outline: none;
+  border-color: #8c857b;
+}
+
+.comment-submit-btn {
+  background-color: #1a1917;
+  color: #ffffff;
+  border: none;
+  padding: 12px 28px;
+  font-family: var(--ff-body);
+  font-weight: 500;
+  font-size: 11px;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
+  cursor: pointer;
+  transition: background-color 0.3s ease;
+}
+
+.comment-submit-btn:hover {
+  background-color: #8c857b;
+}
+
+@media (max-width: 768px) {
+  .comment-form-row {
+    grid-template-columns: 1fr;
+    gap: 15px;
+  }
+}
+
+/* ==========================================================================
+   MOBILE PREVIEW ONLY (16:9 Video & Hero Banner)
+   ========================================================================== */
+
+/* Apply only when the page is viewed in a mobile device or emulation */
+.is-mobile .home-hero {
+  height: 60vh !important;
+  min-height: 400px !important;
+}
+
+.is-mobile .home-video-fixed-wrapper .hero-video-bg iframe {
+  width: 300% !important;
+  height: 100% !important;
+  min-width: 177.78% !important;
+  min-height: 100% !important;
+  top: 50% !important;
+  left: 50% !important;
+  transform: translate(-50%, -50%) !important;
+}
+
+.is-mobile .hero-video-bg iframe {
+  width: 100% !important;
+  height: 100% !important;
+  min-width: auto !important;
+  min-height: auto !important;
+  top: 0 !important;
+  left: 0 !important;
+  transform: none !important;
+}
+
+.is-mobile .home-hero-content {
+  margin-top: 0 !important;
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+  height: 100%;
+}
+
+.is-mobile .gallery-page-hero {
+  height: 56.25vw !important;
+  min-height: auto !important;
+  width: 100vw !important;
+}
+
+.is-mobile .gallery-hero-img-bg img {
+  width: 100% !important;
+  height: 100% !important;
+  min-width: auto !important;
+  min-height: auto !important;
+  top: 0 !important;
+  left: 0 !important;
+  transform: none !important;
+}
+
+.is-mobile .gallery-hero-content {
+  padding-top: 100px !important;
+  padding-bottom: 40px !important;
+  height: 100%;
+  justify-content: space-between;
+}
+
+.is-mobile .films-video-hero,
+.is-mobile .films-hero {
+  height: 50vh !important;
+  min-height: 350px !important;
+  width: 100vw !important;
+}
+
+.is-mobile .films-video-hero .hero-video-bg iframe,
+.is-mobile .films-hero .hero-video-bg iframe {
+  width: 177.78vh !important;
+  height: 100vh !important;
+  min-width: 100% !important;
+  min-height: 100% !important;
+  top: 50% !important;
+  left: 50% !important;
+  transform: translate(-50%, -50%) !important;
+}
+
+.is-mobile .films-hero .page-hero-content {
+  height: 100%;
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+}
+
+.is-mobile .films-video-hero .films-hero-overlay-content {
+  position: absolute !important;
+  left: 5% !important;
+  bottom: 12% !important;
+  height: auto !important;
+  display: block !important;
+  padding: 0 !important;
+}
+
+.is-mobile .films-grid {
+  display: flex !important;
+  flex-direction: column !important;
+  gap: 5rem !important;
+  padding: 0 1rem !important;
+}
+
+.is-mobile .film-card,
+.is-mobile .film-card:nth-child(even) {
+  flex-direction: column !important;
+  gap: 2rem !important;
+}
+
+.is-mobile .film-card-image {
+  width: 100% !important;
+  aspect-ratio: 16 / 9 !important;
+  margin-bottom: 0 !important;
+  flex-shrink: unset !important;
+}
+
+.is-mobile .film-card-info {
+  width: 100% !important;
+  max-width: 100% !important;
+  text-align: center !important;
+  padding: 0 0.5rem !important;
+}
+
+.is-mobile .detail-video-wrapper,
+.is-mobile .lightbox-video-wrapper {
+  height: 56.25vw !important;
+  min-height: auto !important;
+  width: 100% !important;
+}
+
+.is-mobile .detail-video-wrapper iframe,
+.is-mobile .lightbox-video-wrapper iframe {
+  width: 100% !important;
+  height: 100% !important;
+  position: absolute;
+  top: 0 !important;
+  left: 0 !important;
+}
+
+.is-mobile .nav-link,
+.is-mobile .nav-cta {
+  font-size: 22px !important;
+}
+
+@media (hover: none) and (pointer: coarse) {
+
+  .custom-cursor,
+  .custom-cursor-dot {
+    display: none !important;
+  }
+}
+
+/* ═══════════════ TESTIMONIALS SECTION ═══════════════ */
+
+.testimonials-section {
+  padding: var(--space-xl) 0;
+  background-color: var(--clr-bg-deep);
+  position: relative;
+  overflow: hidden;
+  border-top: 1px solid var(--clr-border-light);
+  border-bottom: 1px solid var(--clr-border-light);
+}
+
+.testimonials-header {
+  text-align: center;
+  margin-bottom: var(--space-lg);
+}
+
+.testimonials-marquee-container {
+  width: 100%;
+  overflow: hidden;
+  padding: 1.5rem 0;
+  position: relative;
+}
+
+/* Linear gradient overlays on edges for soft fade out */
+.testimonials-marquee-container::before,
+.testimonials-marquee-container::after {
+  content: "";
+  position: absolute;
+  top: 0;
+  bottom: 0;
+  width: 150px;
+  z-index: 2;
+  pointer-events: none;
+}
+
+.testimonials-marquee-container::before {
+  left: 0;
+  background: linear-gradient(to right, var(--clr-bg-deep) 0%, rgba(243, 236, 226, 0) 100%);
+}
+
+.testimonials-marquee-container::after {
+  right: 0;
+  background: linear-gradient(to left, var(--clr-bg-deep) 0%, rgba(243, 236, 226, 0) 100%);
+}
+
+.testimonials-marquee-row {
+  display: flex;
+  width: max-content;
+  flex-wrap: nowrap;
+  gap: 1.5rem;
+}
+
+.marquee-track {
+  display: flex;
+  flex-shrink: 0;
+  gap: 1.5rem;
+  animation: marquee-scroll 40s linear infinite;
+}
+
+/* Pause marquee scrolling when any card or the row is hovered */
+.testimonials-marquee-row:hover .marquee-track {
+  animation-play-state: paused;
+}
+
+@keyframes marquee-scroll {
+  0% {
+    transform: translateX(0);
+  }
+
+  100% {
+    transform: translateX(calc(-100% - 1.5rem));
+  }
+}
+
+/* Testimonial Card Styling - Liquid Glass Aesthetic */
+.testimonials-marquee-row .testimonial-card {
+  width: 20rem;
+  flex-shrink: 0;
+  cursor: pointer;
+  background: transparent !important;
+  border: none !important;
+  padding: 0 !important;
+  transition: transform 0.4s var(--ease-out-expo), box-shadow 0.4s ease;
+}
+
+.testimonials-marquee-row .testimonial-card:hover {
+  transform: translateY(-8px) scale(1.02);
+}
+
+.testimonial-card-inner {
+  padding: 2.5rem 2rem;
+  background: rgba(255, 255, 255, 0.15);
+  border: 1px solid rgba(255, 255, 255, 0.4);
+  border-radius: 24px;
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
+
+  /* Glass card box shadows based on react liquid glass card */
+  box-shadow:
+    0 4px 30px rgba(0, 0, 0, 0.03),
+    inset 0 1px 1px rgba(255, 255, 255, 0.8),
+    inset 0 -1px 1px rgba(0, 0, 0, 0.1);
+
+  height: 100%;
+  display: flex;
+  flex-direction: column;
+  justify-content: space-between;
+  gap: 1.5rem;
+}
+
+.testimonial-card-header {
+  display: flex;
+  align-items: center;
+  gap: 1rem;
+}
+
+.testimonial-avatar {
+  width: 3.125rem;
+  height: 3.125rem;
+  border-radius: 50%;
+  object-fit: cover;
+  border: 2px solid #ffffff;
+  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
+}
+
+.testimonial-meta {
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+}
+
+.testimonial-name {
+  font-family: var(--ff-display);
+  font-size: 14px;
+  font-weight: 600;
+  letter-spacing: 0.05em;
+  margin: 0;
+  text-transform: none;
+  color: var(--clr-text);
+}
+
+.testimonial-role {
+  font-family: var(--ff-body);
+  font-size: 11px;
+  color: var(--clr-text-muted);
+  margin-top: 2px;
+}
+
+.testimonial-content {
+  font-family: var(--ff-body);
+  font-size: 13px;
+  line-height: 1.6;
+  color: var(--clr-text);
+  margin: 0;
+  display: -webkit-box;
+  -webkit-line-clamp: 3;
+  -webkit-box-orient: vertical;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  font-style: italic;
+}
+
+.testimonial-rating {
+  display: flex;
+  gap: 3px;
+}
+
+.testimonial-rating .star {
+  color: #fff200;
+  font-size: 14px;
+}
+
+/* Testimonials Modal Popup Styling */
+.testimonial-modal {
+  position: fixed;
+  inset: 0;
+  z-index: 99999;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  opacity: 0;
+  visibility: hidden;
+  pointer-events: none;
+  transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1), visibility 0.6s, background-color 0.8s ease;
+  background-color: #070707;
+  overflow-y: auto;
+  /* Allow scrolling if text is long */
+}
+
+.testimonial-modal.active {
+  opacity: 1;
+  visibility: visible;
+  pointer-events: auto;
+}
+
+.testimonial-modal-overlay {
+  position: absolute;
+  inset: 0;
+  background: transparent;
+  z-index: 1;
+  cursor: pointer;
+}
+
+/* Background Watermarks */
+.testimonial-modal-watermarks {
+  position: absolute;
+  inset: 0;
+  display: flex;
+  justify-content: space-between;
+  padding: 0 8%;
+  pointer-events: none;
+  z-index: 1;
+  overflow: hidden;
+  user-select: none;
+}
+
+.watermark-column {
+  display: flex;
+  flex-direction: column;
+  justify-content: space-around;
+  height: 100%;
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 14vw;
+  font-weight: 400;
+  color: rgba(255, 255, 255, 0.015);
+  text-transform: uppercase;
+  line-height: 0.95;
+}
+
+.watermark-left {
+  align-items: flex-start;
+}
+
+.watermark-right {
+  align-items: flex-end;
+}
+
+.testimonial-modal-container {
+  position: relative;
+  width: 100%;
+  min-height: 100%;
+  height: auto;
+  z-index: 2;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  padding: 6rem 8rem;
+  /* Space so overlay contents aren't squeezed */
+  box-sizing: border-box;
+  transform: scale(0.97);
+  opacity: 0;
+  transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1);
+}
+
+.testimonial-modal.active .testimonial-modal-container {
+  transform: scale(1);
+  opacity: 1;
+}
+
+.testimonial-modal-layout {
+  display: flex;
+  width: 100%;
+  max-width: 1200px;
+  height: auto;
+  /* Adjust dynamically to prevent cutoff */
+  align-items: center;
+  justify-content: space-between;
+  gap: 6rem;
+  z-index: 3;
+}
+
+.testimonial-modal-left {
+  flex: 1.3;
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+  text-align: center;
+}
+
+.testimonial-modal-text-content {
+  max-width: 650px;
+  margin: 0 auto;
+}
+
+.testimonial-modal-text {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: clamp(1rem, 1.8vw, 1.35rem);
+  /* Scaled slightly down to fit better */
+  line-height: 1.7;
+  color: rgba(255, 255, 255, 0.85);
+  margin: 0 0 2.5rem 0;
+  font-weight: 300;
+  letter-spacing: -0.01em;
+  white-space: pre-line;
+  /* Respect paragraph newlines */
+}
+
+.testimonial-modal-author {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+}
+
+.testimonial-modal-love {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 16px;
+  color: rgba(255, 255, 255, 0.4);
+  font-style: italic;
+  margin-bottom: 0.5rem;
+  letter-spacing: 0.05em;
+}
+
+.testimonial-modal-name {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 28px;
+  font-weight: 400;
+  letter-spacing: 0.08em;
+  color: #ffffff;
+  margin: 0 0 0.5rem 0;
+}
+
+.testimonial-modal-role {
+  font-family: var(--ff-body);
+  font-size: 11px;
+  letter-spacing: 0.2em;
+  text-transform: uppercase;
+  color: rgba(255, 255, 255, 0.35);
+  margin: 0 0 1rem 0;
+}
+
+.testimonial-modal-rating {
+  display: flex;
+  gap: 6px;
+}
+
+.testimonial-modal-rating .star {
+  color: rgba(255, 255, 255, 0.7);
+  font-size: 12px;
+  letter-spacing: 2px;
+}
+
+.testimonial-modal-right {
+  flex: 0.7;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+}
+
+.testimonial-oval-wrapper {
+  position: relative;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+}
+
+.testimonial-oval-frame {
+  width: 310px;
+  height: 460px;
+  border-radius: 20px;
+  /* Rectangular frame instead of oval */
+  overflow: hidden;
+  border: 1px solid rgba(255, 255, 255, 0.12);
+  box-shadow: 0 35px 70px rgba(0, 0, 0, 0.6);
+  background-color: #0d0d0d;
+}
+
+.testimonial-modal-avatar {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  filter: none;
+  /* Colorful image */
+  transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
+}
+
+.testimonial-oval-frame:hover .testimonial-modal-avatar {
+  transform: scale(1.03);
+}
+
+.testimonial-oval-name {
+  position: absolute;
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 24px;
+  font-weight: 300;
+  color: rgba(255, 255, 255, 0.8);
+  letter-spacing: 0.15em;
+  pointer-events: none;
+  white-space: nowrap;
+}
+
+.testimonial-oval-name-left {
+  left: -80px;
+  transform: rotate(-90deg);
+  transform-origin: center;
+}
+
+.testimonial-oval-name-right {
+  right: -80px;
+  transform: rotate(90deg);
+  transform-origin: center;
+}
+
+.testimonial-modal-close {
+  position: fixed;
+  top: 2rem;
+  right: 2rem;
+  background: transparent;
+  border: none;
+  width: 48px;
+  height: 48px;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  cursor: pointer;
+  z-index: 100001;
+  /* Layer over fixed modal wrapper */
+  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), color 0.4s ease;
+  color: rgba(255, 255, 255, 0.6);
+}
+
+.testimonial-modal-close:hover {
+  transform: rotate(90deg);
+  color: #ffffff;
+}
+
+.testimonial-modal-close svg {
+  width: 22px;
+  height: 22px;
+}
+
+@media (max-width: 991px) {
+  .testimonial-modal {
+    align-items: flex-start;
+    /* Prevent vertical centering cutoff on overflow */
+  }
+
+  .testimonial-modal-close {
+    top: 1.5rem;
+    right: 1.5rem;
+    width: 60px;
+    height: 60px;
+    color: #ffffff;
+    /* More prominent close button */
+  }
+
+  .testimonial-modal-close svg {
+    width: 32px;
+    height: 32px;
+  }
+
+  .testimonial-modal-container {
+    padding: 6rem 2rem 3rem 2rem;
+    height: auto;
+    min-height: 100%;
+    overflow-y: auto;
+    display: block;
+    transform: translateY(20px) scale(1);
+    opacity: 0;
+    transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1);
+  }
+
+  .testimonial-modal.active .testimonial-modal-container {
+    transform: translateY(0) scale(1);
+    opacity: 1;
+  }
+
+  .testimonial-modal-layout {
+    flex-direction: column-reverse;
+    gap: 4rem;
+    height: auto;
+    padding-bottom: 2rem;
+  }
+
+  .testimonial-modal-left {
+    padding-right: 0;
+    text-align: center;
+  }
+
+  .testimonial-modal-text {
+    font-size: 22px;
+    /* Bigger review text */
+    line-height: 1.6;
+    margin-bottom: 2.5rem;
+  }
+
+  .testimonial-modal-love {
+    font-size: 18px;
+    /* Bigger "Love" label */
+  }
+
+  .testimonial-modal-name {
+    font-size: 32px;
+    /* Bigger couple's name */
+  }
+
+  .testimonial-modal-role {
+    font-size: 13px;
+    /* Bigger role text */
+  }
+
+  .testimonial-modal-rating .star {
+    font-size: 16px;
+    /* Bigger rating stars */
+  }
+
+  .testimonial-oval-frame {
+    width: 200px;
+    height: 310px;
+    border-radius: 12px;
+  }
+
+  .testimonial-oval-name-left {
+    left: -60px;
+    font-size: 18px;
+  }
+
+  .testimonial-oval-name-right {
+    right: -60px;
+    font-size: 18px;
+  }
+}
+
+/* ==========================================================================
+   ALTERNATING SECTION COLOR UTILITIES (PROFESSIONAL EDITORIAL THEMES)
+   ========================================================================= */
+
+/* 1. Linen Cream Theme (Light, Warm, Default) */
+.section--cream {
+  background-color: var(--clr-brand-cream) !important;
+  color: var(--clr-brand-dark) !important;
+}
+
+.section--cream h1,
+.section--cream h2,
+.section--cream h3,
+.section--cream .section-title {
+  color: var(--clr-brand-red) !important;
+}
+
+.section--cream p,
+.section--cream span,
+.section--cream .section-subtitle {
+  color: var(--clr-text-muted) !important;
+}
+
+.section--cream .section-label {
+  color: var(--clr-brand-sage) !important;
+}
+
+/* 2. Sage Green Theme (Organic, Midtone) */
+.section--sage {
+  background-color: var(--clr-brand-sage) !important;
+  color: var(--clr-bg-warm) !important;
+}
+
+.section--sage h1,
+.section--sage h2,
+.section--sage h3,
+.section--sage .section-title,
+.section--sage .creations-title,
+.section--sage .creations-subtitle,
+.section--sage .creations-desc,
+.section--sage .fs-feed-title,
+.section--sage .fs-feed-label {
+  color: var(--clr-bg-warm) !important;
+}
+
+.section--sage p,
+.section--sage span,
+.section--sage .section-subtitle,
+.section--sage .creations-subtitle,
+.section--sage .fs-feed-text,
+.section--sage .fs-feed-tag {
+  color: rgba(248, 245, 235, 0.85) !important;
+}
+
+.section--sage .section-label,
+.section--sage .fs-feed-label {
+  color: var(--clr-brand-cream) !important;
+}
+
+/* 3. Deep Olive Charcoal Theme (Dark, Moody) */
+.section--dark {
+  background-color: var(--clr-brand-dark) !important;
+  color: var(--clr-brand-cream) !important;
+}
+
+.section--dark h1,
+.section--dark h2,
+.section--dark h3,
+.section--dark .section-title,
+.section--dark .stories-title,
+.section--dark .story-couple-names {
+  color: var(--clr-brand-cream) !important;
+}
+
+.section--dark p,
+.section--dark span,
+.section--dark .section-subtitle,
+.section--dark .stories-subtitle,
+.section--dark .story-description,
+.section--dark .story-label {
+  color: rgba(239, 233, 215, 0.85) !important;
+}
+
+.section--dark .section-label {
+  color: var(--clr-brand-sage) !important;
+}
+
+.section--dark .story-btn {
+  color: var(--clr-brand-cream) !important;
+  border-color: var(--clr-brand-cream) !important;
+}
+
+.section--dark .story-btn:hover {
+  background: var(--clr-brand-cream) !important;
+  color: var(--clr-brand-dark) !important;
+}
+
+/* 4. Crimson Red Theme (Bold Call to Action) */
+.section--crimson {
+  background-color: var(--clr-brand-red) !important;
+  color: var(--clr-brand-cream) !important;
+}
+
+.section--crimson h1,
+.section--crimson h2,
+.section--crimson h3,
+.section--crimson .section-title {
+  color: var(--clr-brand-cream) !important;
+}
+
+.section--crimson p,
+.section--crimson span,
+.section--crimson .section-subtitle {
+  color: rgba(239, 233, 215, 0.85) !important;
+}
+
+.section--crimson .section-label {
+  color: var(--clr-brand-sage) !important;
+}
+
+.section--crimson .btn-primary {
+  color: var(--clr-brand-cream) !important;
+  border-color: var(--clr-brand-cream) !important;
+}
+
+.section--crimson .btn-primary:hover {
+  background: var(--clr-brand-cream) !important;
+  color: var(--clr-brand-red) !important;
+}
+
+/* ---------- Careers Page Marquee & Floating Portraits ---------- */
+
+.careers-header-section {
+  position: relative;
+  width: 100%;
+  height: 70vh;
+  min-height: 550px;
+  background-color: var(--clr-bg);
+  overflow: hidden;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  padding-top: 180px;
+}
+
+.careers-marquee-container {
+  position: absolute;
+  width: 100%;
+  left: 0;
+  top: 55%;
+  transform: translateY(-50%);
+  overflow: hidden;
+  white-space: nowrap;
+  pointer-events: none;
+  z-index: 1;
+  display: flex;
+  flex-direction: column;
+  gap: 0.1rem;
+}
+
+.careers-marquee-row {
+  width: 100%;
+  overflow: hidden;
+  white-space: nowrap;
+}
+
+.careers-marquee-track {
+  display: inline-block;
+  font-family: var(--ff-display);
+  font-size: clamp(3.5rem, 8vw, 6.5rem);
+  font-weight: 300;
+  color: #000000;
+  letter-spacing: 0.02em;
+  line-height: 1.0;
+  white-space: nowrap;
+  padding-right: 2rem;
+}
+
+.careers-marquee-row-rtl .careers-marquee-track {
+  animation: careersMarqueeRTL 50s linear infinite;
+}
+
+.careers-marquee-row-ltr .careers-marquee-track {
+  animation: careersMarqueeLTR 55s linear infinite;
+}
+
+.marquee-highlight {
+  color: #000000;
+  font-weight: 300;
+  font-style: normal;
+}
+
+@keyframes careersMarqueeRTL {
+  0% {
+    transform: translate3d(0, 0, 0);
+  }
+
+  100% {
+    transform: translate3d(-50%, 0, 0);
+  }
+}
+
+@keyframes careersMarqueeLTR {
+  0% {
+    transform: translate3d(-50%, 0, 0);
+  }
+
+  100% {
+    transform: translate3d(0, 0, 0);
+  }
+}
+
+.careers-floating-portraits {
+  position: absolute;
+  inset: 0;
+  pointer-events: none;
+  z-index: 2;
+}
+
+.careers-portrait {
+  position: absolute;
+  width: clamp(150px, 20vw, 260px);
+  aspect-ratio: 1 / 1;
+  overflow: hidden;
+  pointer-events: auto;
+  box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
+  transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1), filter 0.6s ease;
+  background-color: var(--clr-bg-deep);
+}
+
+.careers-portrait img {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  filter: grayscale(100%);
+  transition: filter 0.6s ease;
+}
+
+.careers-portrait-left {
+  left: 8%;
+  bottom: 10%;
+  border-radius: 40% 40% 40% 40% / 48% 48% 48% 48%;
+  animation: float-left 9s ease-in-out infinite;
+}
+
+.careers-portrait-right {
+  right: 12%;
+  top: 32%;
+  border-radius: 48% 48% 48% 48% / 40% 40% 40% 40%;
+  animation: float-right 11s ease-in-out infinite;
+}
+
+.careers-portrait:hover {
+  transform: scale(1.04) translateY(-8px);
+}
+
+.careers-portrait:hover img {
+  filter: grayscale(0%);
+}
+
+@keyframes float-left {
+  0% {
+    transform: translateY(0) rotate(0deg);
+  }
+
+  50% {
+    transform: translateY(-15px) rotate(1deg);
+  }
+
+  100% {
+    transform: translateY(0) rotate(0deg);
+  }
+}
+
+@keyframes float-right {
+  0% {
+    transform: translateY(0) rotate(0deg);
+  }
+
+  50% {
+    transform: translateY(15px) rotate(-1deg);
+  }
+
+  100% {
+    transform: translateY(0) rotate(0deg);
+  }
+}
+
+/* Mobile responsive adjustments for careers header */
+@media (max-width: 768px) {
+  .careers-header-section {
+    height: 35vh;
+    min-height: 260px;
+    padding-top: 80px;
+  }
+
+  .careers-portrait {
+    width: clamp(110px, 28vw, 150px);
+  }
+
+  .careers-portrait-left {
+    left: 4%;
+    bottom: 8%;
+  }
+
+  .careers-portrait-right {
+    right: 4%;
+    top: 26%;
+  }
+
+  .careers-marquee-track {
+    font-size: clamp(2rem, 9vw, 3.2rem);
+  }
+
+  .careers-marquee-container {
+    gap: 0.25rem;
+  }
+}
+
+/* ============================================
+   HORIZONTAL TIMELINE SCROLL SECTION (about.html)
+   ============================================ */
+
+.timeline-scroll-container {
+  position: relative;
+  width: 100%;
+  height: 600vh; /* Scroll depth: 6 scenes = 600vh */
+  background-color: var(--clr-bg, #EFE9D7);
+}
+
+.timeline-sticky-wrap {
+  position: sticky;
+  top: 0;
+  height: 100vh;
+  width: 100vw;
+  overflow: hidden;
+}
+
+.timeline-track {
+  display: flex;
+  height: 100%;
+  width: 600vw; /* 6 panels = 600vw total width */
+  will-change: transform;
+  transition: transform 0.1s cubic-bezier(0.25, 1, 0.5, 1);
+}
+
+.timeline-scene {
+  position: relative;
+  width: 100vw;
+  height: 100%;
+  flex-shrink: 0;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  overflow: hidden;
+}
+
+/* Background image & overlay */
+.timeline-scene .scene-bg {
+  position: absolute;
+  inset: 0;
+  background-size: cover;
+  background-position: center;
+  transition: transform var(--dur-slow) var(--ease-smooth);
+}
+
+.timeline-scene:hover .scene-bg {
+  transform: scale(1.03);
+}
+
+/* Grayscale look for the crew photo in Scene 4 */
+.timeline-scene.scene-4 .scene-bg {
+  filter: grayscale(100%) brightness(0.65) contrast(1.05);
+}
+
+.timeline-scene .scene-overlay {
+  position: absolute;
+  inset: 0;
+  background: radial-gradient(circle, rgba(0, 0, 0, 0.35) 0%, rgba(0, 0, 0, 0.75) 100%);
+  z-index: 1;
+}
+
+/* Content block styling */
+.timeline-scene .scene-content {
+  position: relative;
+  z-index: 2;
+  color: #ffffff;
+  max-width: 900px;
+  width: 100%;
+  padding: 0 var(--space-md);
+  text-align: center;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  justify-content: center;
+  gap: 1.5rem;
+}
+
+.timeline-scene .scene-number {
+  font-family: var(--ff-body);
+  font-size: var(--fs-label);
+  font-weight: 500;
+  letter-spacing: 0.4em;
+  text-transform: uppercase;
+  color: #F2DCAA; /* Premium gold accent color */
+  margin-bottom: 0.5rem;
+}
+
+.timeline-scene .scene-title {
+  font-family: var(--ff-display);
+  font-size: clamp(2.2rem, 5.5vw, 4.2rem);
+  font-weight: 100;
+  letter-spacing: 0.12em;
+  text-transform: uppercase;
+  color: #ffffff;
+  margin: 0;
+  line-height: 1.2;
+  text-shadow: 0 4px 15px rgba(0, 0, 0, 0.45);
+}
+
+.timeline-scene .scene-copy-wrap {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 1rem;
+}
+
+.timeline-scene .scene-subtitle {
+  font-family: var(--ff-display);
+  font-size: clamp(1.2rem, 2vw, 1.8rem);
+  font-style: italic;
+  color: #F2DCAA;
+  font-weight: 100;
+  text-transform: none;
+  letter-spacing: 0.05em;
+  margin: 0;
+}
+
+.timeline-scene .scene-text {
+  font-family: 'Playfair Display', Georgia, serif;
+  font-style: italic;
+  font-size: clamp(0.9rem, 1.15vw, 1.15rem);
+  line-height: 2.1;
+  color: rgba(255, 255, 255, 0.9);
+  max-width: 720px;
+  margin: 0 auto;
+  text-align: center;
+  letter-spacing: 0.06em;
+  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.35);
+}
+
+/* Scene 4: Crew Split Layout */
+.timeline-scene.scene-4 .scene-content {
+  max-width: 1100px;
+}
+
+.crew-split-layout {
+  display: grid;
+  grid-template-columns: 1fr 1.2fr;
+  gap: 4rem;
+  align-items: center;
+  width: 100%;
+  margin-top: 1rem;
+  text-align: left;
+}
+
+.crew-left-side .scene-text {
+  text-align: left;
+  line-height: 1.9;
+}
+
+.crew-right-side {
+  border-left: 1px solid rgba(255, 255, 255, 0.15);
+  padding-left: 4rem;
+}
+
+.crew-list-grid {
+  display: grid;
+  grid-template-columns: repeat(2, 1fr);
+  gap: 1.8rem;
+}
+
+.crew-member-info {
+  display: flex;
+  flex-direction: column;
+  gap: 0.25rem;
+}
+
+.crew-member-info .crew-name {
+  font-family: var(--ff-display);
+  font-size: 1.05rem;
+  font-weight: 300;
+  color: #ffffff;
+  letter-spacing: 0.05em;
+}
+
+.crew-member-info .crew-role {
+  font-family: var(--ff-body);
+  font-size: 0.72rem;
+  color: #F2DCAA;
+  font-weight: 400;
+  text-transform: uppercase;
+  letter-spacing: 0.08em;
+}
+
+/* Fallback/responsive vertical layout for mobile */
+@media (max-width: 768px) {
+  .timeline-scroll-container {
+    height: auto;
+  }
+  
+  .timeline-sticky-wrap {
+    position: relative;
+    height: auto;
+    width: 100%;
+    overflow: visible;
+  }
+  
+  .timeline-track {
+    flex-direction: column;
+    width: 100%;
+    height: auto;
+    transform: none !important;
+  }
+  
+  .timeline-scene {
+    width: 100%;
+    height: auto;
+    min-height: 90vh;
+    padding: 6rem 1.5rem;
+  }
+  
+  .crew-split-layout {
+    grid-template-columns: 1fr;
+    gap: 2.5rem;
+    text-align: center;
+  }
+  
+  .crew-left-side .scene-text {
+    text-align: center;
+  }
+  
+  .crew-right-side {
+    border-left: none;
+    padding-left: 0;
+  }
+  
+  .crew-list-grid {
+    grid-template-columns: 1fr;
+    gap: 1.5rem;
+  }
+}
+
+/* ═══════════════════════════════════════════
+   WEBSITE SECURITY & IMAGE THEFT PROTECTION
+   ═══════════════════════════════════════════ */
+
+.security-toast {
+  position: fixed;
+  bottom: 2.5rem;
+  left: 50%;
+  transform: translateX(-50%) translateY(100px);
+  background: rgba(15, 15, 15, 0.92);
+  color: #ffffff;
+  padding: 0.8rem 1.6rem;
+  border-radius: 30px;
+  font-family: var(--ff-body);
+  font-size: 0.85rem;
+  letter-spacing: 0.08em;
+  text-transform: uppercase;
+  z-index: 99999;
+  opacity: 0;
+  pointer-events: none;
+  transition: transform 0.4s var(--ease-smooth), opacity 0.4s var(--ease-smooth);
+  box-shadow: 0 12px 36px rgba(0, 0, 0, 0.25);
+  border: 1px solid rgba(255, 255, 255, 0.12);
+  backdrop-filter: blur(8px);
+  -webkit-backdrop-filter: blur(8px);
+  white-space: nowrap;
+}
+
+.security-toast.show {
+  transform: translateX(-50%) translateY(0);
+  opacity: 1;
+}
+
+/* ═══════════════════════════════════════════
+   LATEST CREATIONS — CAMERA SCROLL GALLERY
+   ═══════════════════════════════════════════ */
+
+.latest-creations-camera-section {
+  position: relative;
+  width: 100%;
+  height: 240vh;
+  /* scroll depth for pinned area */
+  background-color: #7D2826;
+  /* Elegant darker red */
+  overflow: visible;
+  margin: 0;
+  box-sizing: border-box;
+}
+
+.camera-section-sticky {
+  position: sticky;
+  top: 0;
+  height: 100vh;
+  width: 100%;
+  overflow: hidden;
+  display: flex;
+  flex-direction: column;
+  justify-content: flex-start;
+  align-items: center;
+  box-sizing: border-box;
+  padding: 9.5rem 1.5rem 0px;
+  /* Increased padding top to clear the sticky navbar (100px) */
+}
+
+.camera-section-header {
+  margin-bottom: 2rem;
+  z-index: 6;
+  position: relative;
+  text-align: center;
+  max-width: 800px;
+  will-change: transform;
+  backface-visibility: hidden;
+  -webkit-backface-visibility: hidden;
+  transform: translateZ(0);
+}
+
+.camera-section-title {
+  font-family: var(--ff-display);
+  font-size: clamp(2rem, 4vw, 3.2rem);
+  font-weight: 300;
+  letter-spacing: 0.12em;
+  text-transform: uppercase;
+  color: #f2dcaa;
+  /* matching primary gold/cream color */
+  margin-bottom: 0.8rem;
+  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
+}
+
+.camera-section-subtitle {
+  font-family: var(--ff-body);
+  font-size: clamp(0.85rem, 1.1vw, 1rem);
+  color: rgba(255, 255, 255, 0.8);
+  line-height: 1.6;
+  text-shadow: 0 1px 4px rgba(0, 0, 0, 0.15);
+}
+
+.camera-animation-viewport {
+  position: relative;
+  width: 100%;
+  max-width: 1200px;
+  height: 65vh;
+  display: flex;
+  justify-content: center;
+  align-items: flex-end;
+  perspective: 1200px;
+  box-sizing: border-box;
+  margin-top: auto;
+  /* Anchors the viewport to the absolute bottom of the sticky container */
+}
+
+.camera-photos-container {
+  position: absolute;
+  inset: 0;
+  display: flex;
+  justify-content: center;
+  align-items: flex-end;
+  z-index: 2;
+}
+
+/* Polaroid Card definition */
+.camera-photo-card {
+  position: absolute;
+  bottom: 30px;
+  /* Positioned low to align with the camera body slot */
+  left: 50%;
+  margin-left: -95px;
+  /* Center card which is 190px wide */
+  width: 190px;
+  height: 235px;
+  padding: 10px 10px 32px 10px;
+  background-color: #ffffff;
+  border-radius: 2px;
+  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.18), 0 2px 5px rgba(0, 0, 0, 0.1);
+  cursor: pointer;
+  transform-origin: bottom center;
+  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.4s ease, z-index 0.4s;
+  pointer-events: auto;
+  box-sizing: border-box;
+  will-change: transform, opacity;
+  backface-visibility: hidden;
+  -webkit-backface-visibility: hidden;
+  transform-style: preserve-3d;
+}
+
+.camera-photos-container.camera-scrolling .camera-photo-card {
+  transition: none !important;
+}
+
+.camera-photo-inner {
+  width: 100%;
+  height: 100%;
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+}
+
+.camera-photo-inner img {
+  width: 100%;
+  height: 165px;
+  object-fit: cover;
+  border: 1px solid #eaeaea;
+  display: block;
+}
+
+.camera-photo-caption {
+  font-family: 'Cormorant Garamond', Georgia, serif;
+  font-style: italic;
+  font-size: 0.85rem;
+  color: #1a1917;
+  text-align: center;
+  white-space: nowrap;
+  overflow: hidden;
+  text-overflow: ellipsis;
+  padding: 0 4px;
+}
+
+/* Hover effect on Polaroid photo: scale, lift up and straighten */
+.camera-photo-card:hover {
+  transform: translate(var(--hover-tx), var(--hover-ty)) scale(1.18) rotate(0deg) !important;
+  z-index: 20 !important;
+  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.45);
+}
+
+/* Camera Layers */
+.camera-visuals-wrapper {
+  position: absolute;
+  bottom: 0px;
+  /* Anchored exactly at the bottom border of the screen/viewport */
+  left: 50%;
+  transform: translateX(-50%);
+  width: 42vw;
+  /* Webflow proportion-based size */
+  max-width: 650px;
+  min-width: 320px;
+  z-index: 4;
+  /* Higher than photo-cards when stack is starting, custom JS will manage card z-indices as they fan out */
+  pointer-events: none;
+}
+
+.camera-body-img {
+  width: 100%;
+  height: auto;
+  display: block;
+  filter: drop-shadow(0 15px 30px rgba(0, 0, 0, 0.3));
+}
+
+.camera-shadow-img {
+  position: absolute;
+  bottom: -20px;
+  left: 50%;
+  transform: translateX(-50%);
+  width: 120%;
+  height: auto;
+  opacity: 0.8;
+  z-index: -1;
+  pointer-events: none;
+}
+
+/* Responsive adjustments */
+@media (max-width: 900px) {
+  .latest-creations-camera-section {
+    height: 180vh;
+    /* slightly less scroll height needed on tablet/mobile */
+  }
+
+  .camera-section-sticky {
+    padding: 7rem 1.5rem 0px;
+    /* Adjust top padding for tablet navbar */
+  }
+
+  .camera-animation-viewport {
+    height: 55vh;
+  }
+
+  .camera-photo-card {
+    width: 140px;
+    height: 175px;
+    margin-left: -70px;
+    bottom: 30px;
+    /* Align with tablet slot */
+    padding: 7px 7px 22px 7px;
+  }
+
+  .camera-photo-inner img {
+    height: 120px;
+  }
+
+  .camera-photo-caption {
+    font-size: 0.68rem;
+  }
+
+  .camera-visuals-wrapper {
+    width: 55vw;
+    /* Fills tablet viewport correctly */
+    bottom: 0px;
+  }
+}
+
+@media (max-width: 480px) {
+  .latest-creations-camera-section {
+    height: auto;
+    aspect-ratio: 16 / 9;
+    /* Perfect 16:9 ratio in mobile view */
+  }
+
+  .camera-section-sticky {
+    position: relative;
+    height: 100%;
+    width: 100%;
+    padding: 1rem 1rem 0px;
+    overflow: hidden;
+  }
+
+  .camera-section-header {
+    margin-bottom: 0.5rem;
+  }
+
+  .camera-section-title {
+    font-size: 1.1rem;
+    margin-bottom: 0.2rem;
+  }
+
+  .camera-section-subtitle {
+    font-size: 0.62rem;
+  }
+
+  .camera-animation-viewport {
+    height: calc(100% - 3.5rem);
+    align-items: flex-end;
+  }
+
+  .camera-photo-card {
+    width: 75px;
+    height: 95px;
+    margin-left: -37.5px;
+    bottom: 35px;
+    /* Lifted inside compact 16:9 viewport */
+    padding: 4px 4px 15px 4px;
+    border-radius: 1px;
+    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
+  }
+
+  .camera-photo-inner img {
+    height: 65px;
+  }
+
+  .camera-photo-caption {
+    font-size: 0.42rem;
+    padding: 0 1px;
+  }
+
+  .camera-photo-card:hover {
+    transform: translate(var(--hover-tx), var(--hover-ty)) scale(1.15) rotate(0deg) !important;
+  }
+
+  .camera-visuals-wrapper {
+    width: 140px;
+    bottom: 0px;
+    /* Attach camera to the bottom on mobile */
+  }
+}
+
+/* Real mobile device overrides (due to viewport width locked at 1200px) */
+html.is-mobile .latest-creations-camera-section {
+  height: auto;
+  aspect-ratio: 16 / 9;
+  /* Perfect 16:9 ratio in mobile view */
+}
+
+html.is-mobile .camera-section-sticky {
+  position: relative;
+  height: 100%;
+  width: 100%;
+  padding: 5rem 1.5rem 0px;
+  /* Reduced top padding to clear navbar and prevent header overlap */
+  overflow: hidden;
+}
+
+html.is-mobile .camera-animation-viewport {
+  height: calc(100% - 6rem);
+  /* Fit within container */
+  align-items: flex-end;
+  margin-top: auto;
+}
+
+/* Use tablet-sized camera and cards on mobile to suit the 1200px wide canvas */
+html.is-mobile .camera-photo-card {
+  width: 140px;
+  height: 175px;
+  margin-left: -70px;
+  bottom: 30px;
+  padding: 7px 7px 22px 7px;
+  border-radius: 2px;
+  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.18);
+}
+
+html.is-mobile .camera-photo-inner img {
+  height: 120px;
+}
+
+html.is-mobile .camera-photo-caption {
+  font-size: 0.68rem;
+}
+
+html.is-mobile .camera-visuals-wrapper {
+  width: 55vw;
+  bottom: 0px;
+}
+
+/* ==========================================================================
+   AHS-STYLE PRELOADER (FORESHADOW)
+   ========================================================================== */
+
+/* Preloader container overlay with glassmorphism */
+/* ═══════════════════════════════════════════
+   CURTAIN REVEAL LOADING SCREEN
+   ═══════════════════════════════════════════ */
+
+.gradient-loader {
+  position: fixed;
+  inset: 0;
+  width: 100vw;
+  height: 100vh;
+  height: 100dvh;
+  z-index: 999999;
+  overflow: hidden;
+  user-select: none;
+  pointer-events: auto;
+  background-color: #050505;
+}
+
+/* WebGL Gradient Canvas */
+#gradient-canvas {
+  position: absolute;
+  top: 0;
+  left: 0;
+  width: 100%;
+  height: 100%;
+  z-index: 1;
+  display: block;
+}
+
+/* Noise Texture Overlay */
+.gradient-noise {
+  position: absolute;
+  inset: 0;
+  z-index: 2;
+  pointer-events: none;
+  opacity: 0.12;
+  background-image: url("data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAADAAAAAwBAMAAAClLOS0AAAAElBMVEUAAAAAAAAAAAAAAAAAAAAAAADgKxmiAAAABnRSTlMCCgkGBAVJOAVJAAAASklEQVQ4y2NgGAWjYBSMglEwCgY/YGRgZBQUYmJiZGQEkYwMjIyMgoKCjIyMIJKBgRFIMjIyAklGRkYGRkFBYEcwMDIyMjAOUQAA1I4HwVwZAkYAAAAASUVORK5CYII=");
+  background-size: 200px;
+  background-repeat: repeat;
+  mix-blend-mode: overlay;
+}
+
+/* ── Center Content (Name + Enter) ── */
+.gradient-content {
+  position: absolute;
+  top: 50%;
+  left: 50%;
+  transform: translate(-50%, -50%);
+  z-index: 5;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 48px;
+  pointer-events: auto;
+}
+
+.gradient-name-wrap {
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  gap: 0;
+  overflow: hidden;
+}
+
+.gradient-letter {
+  display: inline-block;
+  font-family: var(--ff-display), 'Cormorant Garamond', 'Georgia', serif;
+  font-size: clamp(2rem, 5.5vw, 6rem);
+  font-weight: 300;
+  letter-spacing: 0.3em;
+  color: #EFE9D7;
+  text-shadow:
+    0 2px 20px rgba(0, 0, 0, 0.5),
+    0 0 60px rgba(125, 40, 38, 0.4),
+    0 0 120px rgba(125, 40, 38, 0.15);
+  opacity: 0;
+  transform: translateY(60px);
+  will-change: transform, opacity;
+}
+
+/* ── Enter Button ── */
+.gradient-enter-btn {
+  position: relative;
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 10px;
+  background: none;
+  border: 1px solid rgba(239, 233, 215, 0.15);
+  border-radius: 0;
+  cursor: pointer;
+  padding: 14px 48px;
+  opacity: 0;
+  transform: translateY(20px);
+  will-change: transform, opacity;
+  transition: transform 0.3s ease, border-color 0.4s ease, background 0.4s ease;
+}
+
+.gradient-enter-btn:hover {
+  transform: translateY(-2px) !important;
+  border-color: rgba(239, 233, 215, 0.35);
+  background: rgba(125, 40, 38, 0.15);
+}
+
+.gradient-enter-text {
+  font-family: var(--ff-display), 'Cormorant Garamond', 'Georgia', serif;
+  font-size: clamp(0.75rem, 1.2vw, 1rem);
+  font-weight: 400;
+  letter-spacing: 0.5em;
+  color: #EFE9D7;
+  text-shadow: 0 1px 8px rgba(0, 0, 0, 0.3);
+  transition: letter-spacing 0.4s ease, color 0.3s ease;
+}
+
+.gradient-enter-btn:hover .gradient-enter-text {
+  letter-spacing: 0.7em;
+  color: #fff;
+}
+
+.gradient-enter-line {
+  display: block;
+  width: 50px;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, #EFE9D7, transparent);
+  transition: width 0.4s ease, background 0.3s ease;
+}
+
+.gradient-enter-btn:hover .gradient-enter-line {
+  width: 80px;
+  background: linear-gradient(90deg, transparent, #fff, transparent);
+}
+
+/* ── Mobile adjustments ── */
+@media (max-width: 768px) {
+  .gradient-letter {
+    font-size: clamp(1.6rem, 7vw, 3rem);
+    letter-spacing: 0.2em;
+  }
+
+  .gradient-enter-text {
+    font-size: 0.7rem;
+    letter-spacing: 0.4em;
+  }
+
+  .gradient-content {
+    gap: 30px;
+  }
+
+  .gradient-enter-btn {
+    padding: 12px 36px;
+  }
+}
+
+/* ═══════════════════════════════════════════
+   INTERACTIVE STORIES COMMENTS DRAWER
+   ═══════════════════════════════════════════ */
+
+.story-comments-drawer {
+  width: 100%;
+  margin-top: 20px;
+  padding: 25px;
+  background-color: rgba(26, 25, 23, 0.02);
+  border: 1px solid rgba(26, 25, 23, 0.08);
+  border-radius: 4px;
+  box-shadow: inset 0 2px 8px rgba(0, 0, 0, 0.02);
+  display: none;
+  animation: storyCommentsSlideDown 0.4s cubic-bezier(0.19, 1, 0.22, 1) forwards;
+}
+
+@keyframes storyCommentsSlideDown {
+  from {
+    opacity: 0;
+    transform: translateY(-10px);
+  }
+  to {
+    opacity: 1;
+    transform: translateY(0);
+  }
+}
+
+.story-comments-list {
+  display: flex;
+  flex-direction: column;
+  gap: 15px;
+  margin-bottom: 25px;
+  max-height: 250px;
+  overflow-y: auto;
+  padding-right: 8px;
+}
+
+.story-comments-list::-webkit-scrollbar {
+  width: 4px;
+}
+.story-comments-list::-webkit-scrollbar-track {
+  background: transparent;
+}
+.story-comments-list::-webkit-scrollbar-thumb {
+  background: rgba(26, 25, 23, 0.15);
+  border-radius: 10px;
+}
+
+.story-comment-item {
+  display: flex;
+  gap: 15px;
+  padding: 12px 0;
+  border-bottom: 1px dashed rgba(26, 25, 23, 0.05);
+}
+
+.story-comment-item:last-child {
+  border-bottom: none;
+}
+
+.story-comment-avatar {
+  width: 32px;
+  height: 32px;
+  border-radius: 50%;
+  background-color: rgba(26, 25, 23, 0.06);
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  font-family: var(--ff-body);
+  font-weight: 600;
+  font-size: 12px;
+  color: #8c857b;
+  text-transform: uppercase;
+  flex-shrink: 0;
+}
+
+.story-comment-content {
+  flex-grow: 1;
+}
+
+.story-comment-header {
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  margin-bottom: 4px;
+}
+
+.story-comment-author {
+  font-family: var(--ff-body);
+  font-weight: 600;
+  font-size: 13px;
+  color: #1a1917;
+}
+
+.story-comment-date {
+  font-family: var(--ff-body);
+  font-size: 10px;
+  color: #8c857b;
+}
+
+.story-comment-body {
+  font-family: var(--ff-body);
+  font-size: 13px;
+  line-height: 1.5;
+  color: #555555;
+  margin: 0;
+}
+
+.story-no-comments {
+  font-family: var(--ff-body);
+  font-size: 13px;
+  color: #8c857b;
+  font-style: italic;
+  padding: 15px 0;
+  text-align: center;
+}
+
+/* Comment Form inside Story Card */
+.story-comment-form {
+  border-top: 1px solid rgba(26, 25, 23, 0.08);
+  padding-top: 25px;
+}
+
+.story-comment-form-title {
+  font-family: 'Cormorant Garamond', var(--ff-display);
+  font-size: 1.15rem;
+  font-style: italic;
+  margin-bottom: 15px;
+  color: #1a1917;
+  text-transform: none;
+  letter-spacing: 0.02em;
+}
+
+.story-comment-form-row {
+  display: grid;
+  grid-template-columns: 1fr 1fr;
+  gap: 15px;
+  margin-bottom: 15px;
+}
+
+.story-comment-name,
+.story-comment-email,
+.story-comment-text {
+  width: 100%;
+  padding: 10px 12px;
+  background-color: #ffffff;
+  border: 1px solid rgba(26, 25, 23, 0.12);
+  border-radius: 2px;
+  font-family: var(--ff-body);
+  font-size: 12px;
+  color: #1a1917;
+  transition: border-color 0.3s ease, box-shadow 0.3s ease;
+}
+
+.story-comment-name:focus,
+.story-comment-email:focus,
+.story-comment-text:focus {
+  outline: none;
+  border-color: #8c857b;
+  box-shadow: 0 0 5px rgba(140, 133, 123, 0.1);
+}
+
+.story-comment-form-group {
+  margin-bottom: 15px;
+}
+
+.story-comment-submit-btn {
+  background-color: #1a1917;
+  color: #ffffff;
+  border: none;
+  padding: 10px 22px;
+  font-family: var(--ff-body);
+  font-weight: 500;
+  font-size: 10px;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
+  cursor: pointer;
+  transition: background-color 0.3s ease;
+}
+
+.story-comment-submit-btn:hover {
+  background-color: #8c857b;
+}
+
+/* Comments Toggle Styling */
+.story-comments-toggle {
+  cursor: pointer;
+  display: inline-flex;
+  align-items: center;
+}
+
+.story-comments-toggle svg.comment-icon {
+  margin-right: 5px;
+  stroke: #8c857b;
+  transition: stroke 0.3s ease, transform 0.3s ease;
+}
+
+.story-comments-toggle:hover svg.comment-icon {
+  stroke: #1a1917;
+  transform: scale(1.1);
+}
+
+/* Animations for likes */
+@keyframes heartBeatPop {
+  0% { transform: scale(1); }
+  30% { transform: scale(1.3); }
+  60% { transform: scale(0.9); }
+  100% { transform: scale(1); }
+}
+
+.story-entry-likes.liked svg.heart-icon {
+  animation: heartBeatPop 0.4s ease-out;
+}
+
+@media (max-width: 768px) {
+  .story-comment-form-row {
+    grid-template-columns: 1fr;
+    gap: 10px;
+  }
+  .story-comments-drawer {
+    padding: 15px;
+  }
+}
+
+/* Mobile User-Agent specific overrides for Careers page spacing */
+html.is-mobile .careers-header-section {
+  height: 35vh !important;
+  min-height: 260px !important;
+  padding-top: 80px !important;
+}
+
+html.is-mobile .careers-portrait {
+  width: clamp(110px, 28vw, 150px) !important;
+}
+
+html.is-mobile .careers-portrait-left {
+  left: 4% !important;
+  bottom: 8% !important;
+}
+
+html.is-mobile .careers-portrait-right {
+  right: 4% !important;
+  top: 26% !important;
+}
+
+html.is-mobile .careers-marquee-track {
+  font-size: clamp(2rem, 9vw, 3.2rem) !important;
+}
+
+html.is-mobile .careers-marquee-container {
+  gap: 0.25rem !important;
+}
+
+html.is-mobile .fs-contact-section {
+  padding: 1.5rem 0 var(--space-lg) !important;
+}
+
+/* ============================================
+   OUR CREW SECTION (Visit Team Expansion)
+   ============================================ */
+
+#crew-section {
+  display: none;
+  opacity: 0;
+  background-color: #0c0c0c;
+  padding: 80px 20px 120px 20px;
+  position: fixed;
+  top: 0;
+  left: 0;
+  width: 100vw;
+  height: 100vh;
+  z-index: 99999;
+  overflow-y: auto !important;
+  overflow-x: hidden;
+  box-sizing: border-box;
+  overscroll-behavior: contain;
+  -webkit-overflow-scrolling: touch;
+  scroll-behavior: smooth;
+}
+
+#crew-section.show {
+  display: block !important;
+  animation: slideFadeInModal 0.6s cubic-bezier(0.25, 1, 0.5, 1) forwards;
+}
+
+@keyframes slideFadeInModal {
+  from {
+    opacity: 0;
+    transform: scale(0.95) translateY(20px);
+  }
+  to {
+    opacity: 1;
+    transform: scale(1) translateY(0);
+  }
+}
+
+#crew-close-btn {
+  display: none;
+  opacity: 0;
+  position: fixed;
+  bottom: 40px;
+  right: 40px;
+  z-index: 100000;
+  color: var(--clr-brand-cream);
+  font-family: var(--ff-body), sans-serif;
+  font-size: 0.9rem;
+  font-weight: 500;
+  letter-spacing: 0.15em;
+  text-transform: uppercase;
+  cursor: pointer;
+  background: rgba(15, 15, 15, 0.85);
+  padding: 12px 24px;
+  border-radius: 30px;
+  border: 1px solid rgba(255, 255, 255, 0.15);
+  backdrop-filter: blur(8px);
+  -webkit-backdrop-filter: blur(8px);
+  transition: background 0.3s ease, color 0.3s ease, border-color 0.3s ease, transform 0.3s cubic-bezier(0.25, 1, 0.5, 1);
+  align-items: center;
+  gap: 8px;
+  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
+}
+
+#crew-section.show ~ #crew-close-btn {
+  display: flex !important;
+  animation: fadeInCloseBtn 0.6s ease forwards;
+}
+
+@keyframes fadeInCloseBtn {
+  from {
+    opacity: 0;
+    transform: translateY(15px);
+  }
+  to {
+    opacity: 1;
+    transform: translateY(0);
+  }
+}
+
+#crew-close-btn:hover {
+  background: var(--clr-brand-cream);
+  color: #0c0c0c;
+  border-color: var(--clr-brand-cream);
+  transform: translateY(-3px) scale(1.05);
+  box-shadow: 0 15px 40px rgba(0, 0, 0, 0.5);
+}
+
+#crew-close-btn:active {
+  transform: translateY(-1px) scale(1.01);
+}
+
+/* Always show the desktop banner and hide the mobile banner so the
+   16:9 aspect-ratio is preserved on every viewport size. */
+#crew-section .team-banner.dekstop {
+  display: block !important;
+}
+#crew-section .team-banner.mobile {
+  display: none !important;
+}
+
+#crew-section .team-banner-wrapper {
+  position: relative;
+  aspect-ratio: 16 / 9;
+  border-radius: 20px;
+  background-repeat: no-repeat;
+  background-size: cover;
+  background-position: center;
+  width: 100%;
+  min-height: 450px;
+  overflow: hidden;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  margin-bottom: 50px;
+}
+
+#crew-section .team-banner-wrapper .overlay {
+  position: absolute;
+  inset: 0;
+  background: rgba(0, 0, 0, 0.4);
+  z-index: 1;
+}
+
+#crew-section .our--workshop-wrapper {
+  position: relative;
+  z-index: 2;
+  text-align: center;
+  color: var(--clr-brand-cream) !important;
+}
+
+#crew-section .crew-list {
+  display: grid;
+  grid-template-columns: repeat(4, 1fr);
+  column-gap: 30px;
+  row-gap: 40px;
+  width: 100%;
+  max-width: 1400px;
+  margin: 0 auto;
+  padding: 0 20px;
+  box-sizing: border-box;
+}
+
+#crew-section .crew-list .crew-item {
+  display: block !important;
+  visibility: visible !important;
+  opacity: 1 !important;
+}
+
+#crew-section .crew-box {
+  aspect-ratio: 2 / 3;
+  border-radius: 20px;
+  position: relative;
+  overflow: hidden;
+  width: 100%;
+}
+
+#crew-section .crew-image-item {
+  position: relative;
+  width: 100%;
+  height: 100%;
+  border-radius: 20px;
+  overflow: hidden;
+  transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1);
+  transform-origin: center center;
+}
+
+#crew-section .crew-image-item._2 {
+  transform: rotate(-1.5deg);
+}
+
+#crew-section .crew-image-item:hover {
+  transform: scale(1.03) rotate(0deg) !important;
+  z-index: 5;
+}
+
+#crew-section .image-82 {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  filter: grayscale(100%) brightness(0.85);
+  transition: filter 0.5s cubic-bezier(0.25, 1, 0.5, 1), transform 0.5s cubic-bezier(0.25, 1, 0.5, 1);
+}
+
+#crew-section .crew-image-item:hover .image-82 {
+  filter: grayscale(0%) brightness(1) !important;
+  transform: scale(1.08) !important;
+}
+
+#crew-section .linear-grad {
+  position: absolute;
+  inset: auto 0 0 0;
+  height: 60%;
+  background: linear-gradient(to top, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0) 100%);
+  pointer-events: none;
+  z-index: 2;
+}
+
+#crew-section .content-crew {
+  position: absolute;
+  bottom: 25px;
+  left: 20px;
+  right: 20px;
+  z-index: 3;
+  pointer-events: none;
+}
+
+#crew-section .member-name {
+  color: #FFFFFF !important;
+  font-family: 'Cormorant Garamond', 'Georgia', serif !important;
+  font-size: 1.45rem;
+  font-weight: 300;
+  text-transform: uppercase;
+  letter-spacing: 2px;
+  margin-bottom: 4px;
+}
+
+#crew-section .job-titel {
+  color: var(--clr-brand-cream) !important;
+  font-family: var(--ff-body), sans-serif !important;
+  font-size: 0.85rem;
+  font-weight: 300;
+  letter-spacing: 1px;
+  text-transform: uppercase;
+  margin-bottom: 8px;
+  opacity: 0.95;
+}
+
+#crew-section .job-dis {
+  color: rgba(255, 255, 255, 0.7) !important;
+  font-family: var(--ff-body), sans-serif !important;
+  font-size: 0.78rem;
+  line-height: 1.4;
+  margin: 0;
+  opacity: 0;
+  transform: translateY(10px);
+  transition: opacity 0.4s ease, transform 0.4s ease;
+}
+
+#crew-section .crew-image-item:hover .job-dis {
+  opacity: 1;
+  transform: translateY(0);
+}
+
+@media screen and (max-width: 991px) {
+  #crew-section .team-banner.dekstop {
+    display: block !important;
+  }
+  #crew-section .team-banner.mobile {
+    display: none !important;
+  }
+  #crew-section .team-banner-wrapper {
+    width: 100% !important;
+    height: auto !important;
+    aspect-ratio: 16 / 9 !important;
+    background-image: url('../crew/crew.jpg') !important;
+    border-radius: 20px !important;
+    min-height: auto !important;
+  }
+  #crew-section .crew-list {
+    grid-template-columns: repeat(3, 1fr) !important;
+    column-gap: 15px !important;
+    row-gap: 20px !important;
+    padding: 0 10px !important;
+  }
+  #crew-section .member-name {
+    font-size: 1rem !important;
+  }
+  #crew-section .job-titel {
+    font-size: 0.65rem !important;
+  }
+  #crew-close-btn {
+    top: 20px !important;
+    right: 20px !important;
+    bottom: auto !important;
+    padding: 10px 18px !important;
+    font-size: 0.8rem !important;
+    z-index: 1000000 !important;
+    pointer-events: auto !important;
+  }
+}
+
+@media screen and (max-width: 550px) {
+  #crew-section .team-banner-wrapper {
+    width: 100% !important;
+    height: auto !important;
+    aspect-ratio: 16 / 9 !important;
+    min-height: auto !important;
+  }
+  #crew-section .our--workshop-wrapper {
+    padding: 10px !important;
+  }
+  #crew-section .title-heading {
+    font-size: 1.5rem !important;
+  }
+  #crew-section .paragraph.b-p {
+    font-size: 0.75rem !important;
+    line-height: 1.3 !important;
+  }
+  #crew-section .crew-list {
+    grid-template-columns: repeat(3, 1fr) !important;
+    column-gap: 8px !important;
+    row-gap: 12px !important;
+    padding: 0 5px !important;
+  }
+  #crew-section .member-name {
+    font-size: 1.4rem !important;
+    letter-spacing: 1px !important;
+  }
+  #crew-section .job-titel {
+    font-size: 0.5rem !important;
+    letter-spacing: 0.5px !important;
+  }
+  #crew-section .content-crew {
+    bottom: 8px !important;
+    left: 8px !important;
+    right: 8px !important;
+  }
+}
+
+/* ═══════════════ FIXED VIDEO PARALLAX SYSTEM ═══════════════ */
+.home-video-fixed-wrapper {
+  position: relative;
+  width: 100%;
+  background-color: #0A0A0A;
+  overflow: hidden;
+  clip-path: inset(0 0 0 0);
+  -webkit-clip-path: inset(0 0 0 0);
+}
+
+.home-video-fixed-wrapper .hero-video-bg {
+  position: fixed;
+  top: 0;
+  left: 0;
+  width: 100%;
+  height: 100vh;
+  height: 100dvh;
+  z-index: -1;
+  pointer-events: none;
+  overflow: hidden;
+}
+
+.home-video-fixed-wrapper .hero-video-bg::after {
+  content: '';
+  position: absolute;
+  inset: 0;
+  z-index: 1;
+  background: linear-gradient(
+    to bottom,
+    rgba(0, 0, 0, 0.35) 0%,
+    rgba(0, 0, 0, 0.5) 50%,
+    rgba(0, 0, 0, 0.65) 100%
+  );
+}
+
+.fixed-wrapper-content {
+  position: relative;
+  z-index: 1;
+}
+
+.why-foreshadow-manifesto {
+  background-color: transparent !important;
+  padding: 14rem 0 16rem 0;
+  display: flex;
+  align-items: center;
+}
+
+.manifesto-container {
+  display: flex;
+  flex-direction: row;
+  justify-content: space-between;
+  align-items: flex-start;
+  max-width: 75rem;
+  gap: 4rem;
+  margin: 0 auto;
+  padding: 0 2rem;
+}
+
+.manifesto-left {
+  flex: 0 0 25%;
+}
+
+.manifesto-label {
+  font-family: var(--ff-body), sans-serif;
+  font-size: 0.72rem;
+  font-weight: 500;
+  letter-spacing: 0.22em;
+  text-transform: uppercase;
+  color: var(--clr-brand-sage, #a3b899) !important;
+  opacity: 0.9;
+}
+
+.manifesto-right {
+  flex: 0 0 70%;
+  display: flex;
+  flex-direction: column;
+  align-items: flex-start;
+  text-align: left;
+}
+
+.manifesto-heading {
+  font-family: 'Cormorant Garamond', Georgia, serif;
+  font-size: clamp(2.2rem, 4.4vw, 3.8rem);
+  font-weight: 300;
+  line-height: 1.12;
+  color: #ffffff !important;
+  text-transform: none !important;
+  margin-bottom: 2.8rem;
+  letter-spacing: -0.01em;
+  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
+}
+
+.manifesto-desc-block {
+  max-width: 38.75rem;
+}
+
+.manifesto-desc {
+  font-family: var(--ff-body), sans-serif;
+  font-size: clamp(0.96rem, 1.35vw, 1.08rem);
+  line-height: 1.75;
+  font-weight: 300;
+  color: rgba(255, 255, 255, 0.8) !important;
+  margin-bottom: 1.8rem;
+  text-shadow: 0 1px 8px rgba(0, 0, 0, 0.2);
+}
+
+.manifesto-desc:last-child {
+  margin-bottom: 0;
+}
+
+@media (max-width: 768px) {
+  .why-foreshadow-manifesto {
+    padding: 8rem 0;
+  }
+
+  .manifesto-container {
+    flex-direction: column;
+    gap: 1.8rem;
+  }
+  
+  .manifesto-left {
+    flex: 0 0 100%;
+  }
+  
+  .manifesto-right {
+    flex: 0 0 100%;
+  }
+  
+  .manifesto-heading {
+    margin-bottom: 1.8rem;
+    line-height: 1.15;
+  }
+}
